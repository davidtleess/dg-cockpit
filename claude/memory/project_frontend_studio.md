---
name: frontend-studio-outsider-agent
description: "The \"Studio\" outsider front-end agent lane — purpose, contamination rules, and file locations"
metadata: 
  node_type: memory
  type: project
  originSessionId: 38369f00-9dee-487c-8809-f585ebd3dff6
---

Created 2026-07-14. David runs a 3-agent governed cockpit (tmux session `dynasty`: Claude Code, Codex, Gemini) on dynasty-genius-product, plus a deliberately UNgoverned fourth agent, **Studio** — an opinionated front-end designer persona with dynasty fantasy expertise, launched via `claude` with cwd `~/frontend-studio` (its CLAUDE.md is the persona/bootstrap).

Key rules: Studio is read-only on the repo, must never read the repo's governance/spec/design docs (fresh-eyes covenant), and communicates with the engineers only through David's gate. **Since 2026-07-14 evening, Tower is the transport layer**: David decides ("relay NNN"), Tower delivers the standard relay line to all three crew panes, verifies each landed (retry Enter once — the tmux paste race strands messages), sends Studio a bare mechanical ack ("NNN relayed to crew"), and confirms to David. David is the gate, never the wire; Tower remains the contamination filter in both directions. Insiders may not reject Studio proposals "because policy" — they translate to costs and escalate genuine governance conflicts to David, default-favorable. Files: `~/frontend-studio/PRODUCT_BRIEFING.md` (facts-only merged briefing from three independent agent drafts), `for-david/WELCOME_MESSAGE.md`, `for-david/BUS_PROTOCOL.md`, `proposals/`.

**Why:** The governed agents share blind spots; Studio's value is decorrelated perspective, which contamination destroys. Validated in practice: within its first days David confirmed Studio was "producing real improved recommendations" — first audit yielded 10+ engineer-confirmed defects, an approved prototype (001), and zero governance collisions.

**How to apply:** Never launch Studio inside the repo (repo CLAUDE.md conscripts agents into governance). When helping David relay between lanes, strip governance/spec/process language before anything crosses to Studio. Periodic fresh-session resets keep the perspective fresh (dg rebuilds do this automatically — Studio is window 2 of the dg flight deck). See [[project_dynasty_genius]].

**State as of 2026-07-14 (first cycle):** Studio's audit (proposals/000) produced 12 findings; engineers confirmed S1–S10+S12a-d, refuted S12e and S11b's mechanism, zero governance escalations. S4 (health 503, stale server vs config_version 2) fixed and verified. David's pending ruling: approve "Batch A" defects (S1, S2, S3, S9a, S12a-d, +S7 token leak) now; hold design gaps (S5, S6, S8, S10, S11) for Studio proposals 001–005 which arrive with prototypes. Studio is researching 001 (Morning Tape). Ops note (2026-07-14/15): a Claude Code TUI can freeze (process alive, keys ignored — Enter/Esc/C-u all no-ops; cursor_x=2 with text rendered betrays the stale frame) — struck three times in 24h (Studio ×2, engineer Claude ×1), always at a turn boundary. Recovery is `kill <claude pid>` then `claude --continue` in the same pane/cwd; conversation resumes with full context, but any in-process Workflow run dies and is NOT resumable across the kill (lost a 70/101-agent deep-research run). Suspected root cause: tmux `focus-events off` (Claude Code warns on boot) — fix is `set -g focus-events on` in ~/.tmux.conf, pending David's OK. Diagnose via the session .jsonl transcript (ground truth for what was actually received), never the rendered pane. Closeout lesson: a crew agent can jam overnight on a permission prompt mid-flush — sweep for "Do you want to proceed?" dialogs before declaring the wire clean.

**NEXT-SESSION MORNING BRIEF (closed 2026-07-15 ~09:00; all lanes closed-parked/clean, focus-events fix in ~/.tmux.conf awaiting dg rebuild — verify freezes stop):** David's decision set, in order: (1) Studio S1 — combined design review (universe list w/ model-rank column + question ladder), staged, needs his browser reaction; (2) Studio S2 — 001b relay approval, decision-ready; (3) crew first work item — Batch A REDs in /private/tmp/dg-studio-batch-a vs holistic-stats scope vs 02-amendment-draft-v2 ratification; (4) stray `league_opportunity_latest.{json,md}` uncommitted on docs/product-briefing-claude — commit or discard; (5) relay verdict substance on 000/001 to Studio (it's still owed it). Also pending: David-agenda checkpoint 1 (the two spokesperson one-liners) fires at that session's open. Studio's Bo Nix data-staleness thread resolves via 001b-N7 or David's own Sleeper glance. Loops added since creation: DAVID.md (Studio's durable memory of David's feedback, read at every boot), relay briefs with 60-second summary blocks (no meta in engineer-visible files), design review ritual (Studio opens the browser for David, five-line guide, one question), attention layer (tmux bell→red tab + macOS banners via Claude Notification hook and Codex notify). The chief-of-staff role is institutionalized as **Tower** (David chose the name — air-traffic control over his flight deck): agent definition at ~/.claude/agents/tower.md, launched via David's `dg-tower` alias (cd ~ && claude --agent tower) — its boot ritual is memory + BUS_PROTOCOL.md + pane survey, then tell David the one thing that needs him.
