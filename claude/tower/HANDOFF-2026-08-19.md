# TOWER HANDOFF — 2026-08-19, end of session

Read this before touching Gemini, the autonomy hook, or the freshness config.
Everything below was measured, not inferred. Where something is unverified it says so.

---

## 1. SHIPPED AND VERIFIED TODAY

### The dg-autonomy tool policy — four defects fixed
`~/.gemini/config/plugins/dg-autonomy/scripts/dg-antigravity-tool-policy.mjs`
(live) and `~/dg-cockpit/autonomy/antigravity/dg-autonomy/scripts/...` (source) — **identical**, so
`install.sh`'s hash comparison will not revert them.

| # | Defect | Fix |
|---|---|---|
| 1 | `hooks.json` used `${PLUGIN_ROOT}`, which agy never expands → `Cannot find module '/scripts/...'` | `${HOME}/.gemini/config/plugins/dg-autonomy` |
| 2 | agy sends `TargetFile` (PascalCase); policy read `file_path` → **every write** denied "no verifiable target" | `TARGET_KEYS`, case-insensitive |
| 3 | `roots.length !== 1`; agy reports root + nested folders → **every tool** denied | `collapseWorkspaceRoots()` + Studio guard |
| 4 | agy sends `CommandLine`; policy read `args.command` → **every shell command** denied "malformed" | `COMMAND_KEYS` / `commandString()` |

**#4 REGRESSED ONCE ALREADY.** Another lane rewrote the file at 16:02:27, kept #2 and #3, and dropped
#4 — which is exactly what "Gemini isn't working" meant. It is restored with a loud
`⚠ DO NOT REMOVE` comment. **If Gemini ever stops being able to run commands, check `commandString`
first.**

### A pre-existing security hole closed
The unpatched policy ALLOWED writes to `~/frontend-studio` and `~/.ssh` whenever a single workspace
entry of `[$HOME]` or `[/]` was supplied. `forbiddenRoots = {/, $HOME}` + the Studio containment check
close it. This was **not** introduced by today's work — it was already open.

### Verifier
`~/.claude/tower/bin/verify-tool-policy.sh [file]` — 23 asserted cases, prints `ALL PASS` or refuses.
**Run it before and after ANY policy edit.** Currently ALL PASS on live.

### Scoped launcher
`~/.claude/tower/bin/agy-scoped.sh <ABSOLUTE-path> [agy args...]` — sets `DG_AUTONOMY_WORKTREE`,
refuses `~`, relative paths, non-directories. Needed only when the IDE has two disjoint folders open
(trunk + worktree), which the policy rightly refuses to disambiguate.

### agy permission grants (durable)
`~/.gemini/config/config.json` → `userSettings.globalPermissionGrants.allow`, now 31 entries.
Backup: `~/.claude/tower/backups/agy-config.json.pre-grants`.
Format is `command(<prefix>)`, matched by FULL PREFIX. Created via agy's own dialog option 3
("Persist to settings.json") — that is the only documented way to learn the format.
**Grants apply at agy STARTUP.** `shift+tab` (accept-edits) does NOT persist and must be re-toggled
every session.

---

## 2. HOW TO LAUNCH GEMINI SO IT CAN BUILD

Canonical launch is bare `agy` (`dynasty_flight_deck.sh:41`), and after today's fixes a bare launch
from the trunk works — verified with the exact payload it produces.

For ticket work in a worktree:
```
~/.claude/tower/bin/agy-scoped.sh /Users/davidleess/dg-wt/DG-0NN
# then shift+tab for accept-edits
```
Proven working: ran `git branch`, `git rev-parse`, `echo`, and wrote files in `~/dg-wt/DG-021` on
branch `ticket/DG-021`, unattended, across a restart.

Its `dg-auto` skill REQUIRES the run-record CLI before editing:
`$HOME/.dg-autonomy/bin/dg-autonomy init → record-check → finish`. The CLI exists and is granted.

**Still gated (correctly, by David's boundary):** commit, push, destructive work, external
communication. Verified denying with grants live.

---

## 3. NOT FIXED — NEEDS DAVID

### syspolicyd is wedged. This is the biggest operational problem on the machine.
Climbed 256% → 280% → 326% all afternoon; load average went 10 → 17 → **71.84 at 16:36**.
Every process launch queues behind Gatekeeper assessment. **Hooks have a 10-second timeout, so this
makes every lane fail intermittently with errors indistinguishable from config bugs.** Much of today's
"Gemini is blocked" was this.
```
sudo killall syspolicyd     # respawns clean; needs David's sudo
```
Tower killed 28 orphaned `agy` processes (all ppid=1, one 66-second burst) which halved load 35 → 17,
but syspolicyd itself is untouched and is the real driver.

### Four verified freshness/backup defects — all PROPOSED PRODUCT CHANGES, none started
Tower verified these independently from source; two reproduced first-hand. Details on
`PRODUCT-HEALTH-BOARD.md` under 2026-08-19 15:2x.

1. **A failed daily refresh reports "fresh."** Measured through the real evaluator:
   `pvo_refresh aborted → fresh (mtime_fresh)`, `feature_refresh blocked → fresh
   (embedded_timestamp_fresh)`. Cause: `system_health_models.py:509` guards every status gate behind
   `status_field is not None`, and **5 artifacts omit it** (`pvo_refresh`, `feature_refresh`,
   `what_changed`, `roster_capacity`, `league_opportunity`) — the ledger filed only 2.
   **Fix is config-only** and both named producers write a status on every exit path, so they qualify.
   ⇒ **Highest value, lowest risk item on the board. Do this one first.**
2. Backup failure is invisible on the two top surfaces — `system_tier_readiness.py` and
   `system_health.py` have **zero** references to backup, while `system_capture_health.py` does fold
   it in.
3. Backup health accepts a future-dated, internally failed marker. Reproduced:
   `status=completed, sha256_verified=true, finished_at=now+3d, failures=["restore drill mismatch"]`
   → `status: ok, reasons: []`. `failures` is echoed but never added to `reasons`; staleness is
   `now - finished_at`, negative for a future date. **This sits on Tower's own backup duty.**
4. `inspect_market_divergence_refresh_status` never reads its marker's own status. Not wired into the
   live health route (confirmed), so lower priority.

NOT verified, and must not be promoted without reachability testing: the realized-outcome scorecard
route, the roster-pool caveat, the roster-audit envelope.

### Ballot
3 of 4. Gemini Consultant slot still empty. **Q3: (b) closes 3-1, (c) ties 2-2 and escalates to David.**
Q1 and Q2 are CLOSED at (a). The Consultant on ttys025 is an IDE terminal — **Tower has no channel to
it**; only David can launch it.

---

## 4. TOWER'S OWN FAILURES TODAY — read these, they are the pattern

1. **Theorised instead of instrumenting.** Burned hours on wrong causes (empty workspacePaths, cwd,
   an unexpanded tilde). Each real cause appeared within two minutes of logging the live hook payload
   (`DG_HOOK_DEBUG=1`) or reading the lane's own pane. **Instrument first.**
2. **Tested an adjacent capability and reported it as the one asked about.** Claimed "Gemini can
   build" from a matrix that never once tested `run_command`.
3. **Shipped a command with two defects** (`~` that `path.resolve` does not expand, and no note that
   it was a shell command) — it was pasted into agy and spawned nested sessions.
4. **Installed an unverified security edit, then reverted it** on discovering it would have made
   `$HOME` an authorized root and opened the Studio wall.
5. **Asked David to paste error text that was already on disk.** `lsof -p <pid>` maps a process to its
   transcript; agy writes to `~/.gemini/antigravity-cli/brain/*/logs/`.
6. **Nearly killed its own processes** — `ps | grep agy` matches Tower's own shell whenever its command
   line contains the string. Confirm `comm` and parentage before killing anything.

## 5. Watcher
`~/.claude/tower/bin/ledger-watch.sh` is STOPPED — Tower stopped it because it execs `find`/`grep`/
`stat` every 30s, which is the wrong thing to add while Gatekeeper is saturated.
Restart AFTER syspolicyd is cleared:
```
nohup ~/.claude/tower/bin/ledger-watch.sh >/dev/null 2>&1 &
```

---

## 6. ⚠ WARNING FOUND AT SESSION END — DO NOT COMMIT FROM ~/dg-wt/DG-021

`git -C ~/dg-wt/DG-021 status --porcelain` reports **72 tracked files as DELETED**, all under
`app/data`: 26 under `identity`, 9 `backtest/trust_surface/latest`, 8 `backtest/model_cards`
(including `QB_model_card.json` and the calibration reports), the divergence ledgers, `sources/`
schedules, and several phase directories.

**This is NOT a symlink artifact** — `app/data` in that worktree is a real directory
(`ls -ld` shows `drwxr-xr-x`, not a symlink), so the files are genuinely absent from the worktree.
AGENT-HOOK.md §4 expects `dg-work.sh` to SYMLINK `app/data`; in DG-021 it did not.

**No data has been lost: the trunk copies are intact** (verified
`app/data/backtest/model_cards/QB_model_card.json` present in `~/dynasty-genius-product`).

**The risk is a commit.** A commit from DG-021 would record the deletion of 72 model artifacts.
`git commit` is human-gated by the autonomy boundary, which means **David is the only one who can
trigger it** — so this warning is for him specifically. Either restore/symlink `app/data` in that
worktree before any commit, or commit only explicit paths (AGENT-HOOK.md §3 already forbids
`git add -A`).

The genuine work in DG-021 is exactly two files:
`M src/dynasty_genius/pvo_assembler.py` · `M tests/contract/test_phase15_blend.py`

Other worktrees for contrast: DG-022 has 19 modified (API route + regenerated frontend client),
DG-023 has `M scripts/run_feature_refresh.py` + `A tests/contract/test_stream_provenance_status_red.py`
staged — that one is a test-first attack on the same producer as freshness blocker #1.

---

## 7. FRESHNESS CONFIG FIX — PATCH READY, NOT APPLIED, NOT VERIFIED

David said "fix the freshness config" at ~17:00. Tower built the patch but **did NOT apply it**, for two
reasons, in order of importance:

**(a) It could not be verified.** The machine stopped being able to run Python:
```
.venv/bin/python3.14 -c "import pydantic"
  ImportError: dlopen(.../pydantic_core...so): code signature ... not valid for use in process:
  library load mig callout failed
```
That is Gatekeeper/`syspolicyd` failing to validate a signature, so the native extension cannot load.
Tower will not ship an unverified change to the health surface — that is exactly the mistake it made and
reverted earlier today with the tool policy.

**(b) It is a product-repo edit**, which Tower's charter forbids. David authorised it verbally; the
patch is staged so he or a lane can land it through the normal worktree/ticket path.

**The patch:** `~/.claude/tower/staging/report_freshness.candidate.json` — a full replacement for
`app/config/report_freshness.json`. The complete diff is two artifacts, nothing else:
```
pvo_refresh:      + "status_field": "status",  "success_status": "ok"
feature_refresh:  + "status_field": "status",  "success_status": ["ok","noop","candidate_ready"]
```

**IMPORTANT CORRECTION Tower made while doing this.** Tower earlier told David that FIVE artifacts
should declare `status_field`. **That was wrong — only TWO should.** The other three producers
(`run_what_changed_report.py`, `run_roster_capacity_audit.py`, `build_league_opportunity_map.py`)
write **no `status` field at all**, so declaring it for them would make the evaluator report
`malformed_status` and MANUFACTURE FALSE FAILURES. The ledger's original "2 producer types" was right.
The other three are a separate, real gap — their failure is genuinely illegible — but fixing that is a
PRODUCER change, not a config change.

**Evidence the two chosen values are correct:**
- On-disk artifacts both carry a top-level `status` (currently `ok`):
  `app/data/model_capture/pvo_refresh_latest_report.json`,
  `app/data/features_runtime/feature_refresh_latest_report.json`.
- `run_pvo_refresh.py` writes only `ok` (3x) and `aborted` (5x).
- `feature_refresh_runner.py` writes `ok` (:162), `noop` (:109), `candidate_ready` (:132,:140),
  `blocked` (:174). The `"unavailable"` in `run_feature_refresh.py:123` is a PER-STREAM provenance
  value, NOT the artifact status — do not add it to `success_status`.
- Schema copied from the three artifacts that already declare it (`realized_outcome` shows the list
  form; `market_divergence`/`league_capture` show the scalar form).
- `failure_reason_field` deliberately OMITTED — it is optional, and Tower could not confirm a
  top-level reason key on either artifact.

**STILL TO DO before landing:** run the evaluator against the candidate and confirm
(1) `aborted`/`blocked` now grade as producer-failed, and (2) `ok`/`noop`/`candidate_ready` still grade
fresh so no false alarm is introduced. The probe Tower wrote for this is in the session record; it needs
a machine that can `import pydantic`.

## 8. ⛔ MACHINE STATE AT SESSION END — WORSE THAN A SLOWDOWN
```
17:04  load averages: 145.92  63.49  36.70
syspolicyd 62%   (peaked 326%)
python3.14 -c "import pydantic"  -> ImportError, code signature not valid
curl /api/health                 -> HTTP=000 (no response)
```
**The product is effectively down and Python cannot load native extensions.** Any launchd producer
firing in this state will fail, and the freshness surfaces cannot be trusted until it is cleared.
Tower told David at ~16:58 that the killall was no longer needed — **that was wrong**; load was
momentarily 11.6 and then went to 145. First action next session:
```
sudo killall syspolicyd
uptime            # confirm load falls
.venv/bin/python3.14 -c "import pydantic; print('ok')"   # confirm libraries load again
curl -s localhost:8000/api/health | head -c 200          # confirm the API answers
```

### Addendum, 18:57 — patch structurally validated; machine still broken

The freshness patch is now de-risked as far as is possible without pydantic. Checked with plain
`python3` (which works — only the venv's signed native extension fails):
```
candidate parses as JSON            OK
config_version 2 -> 2               unchanged
artifact count 8 -> 8               unchanged
artifact ids                        identical
pvo_refresh      added {status_field: status, success_status: ok}          removed [] changed {}
feature_refresh  added {status_field: status, success_status: [ok,noop,candidate_ready]}  removed [] changed {}
```
Nothing removed, nothing altered, only the two intended additions. **What remains unverified is
SEMANTIC only:** that `aborted`/`blocked` now grade producer-failed and `ok`/`noop`/`candidate_ready`
still grade fresh. That needs `import pydantic`, i.e. a working syspolicyd.

**Machine at 18:57, ~2 hours after the first alarm — NOT self-healing:**
```
load 6.69 (recovered)   syspolicyd 338%  (highest reading all day)
.venv/bin/python3.14 -c "import pydantic"  -> still ImportError, code signature not valid
curl /api/health                           -> still HTTP=000
```
Load falling is misleading — the breakage is signature validation, not CPU. `sudo killall syspolicyd`
is still the first action, and nothing product-side can be trusted or verified until it is done.
