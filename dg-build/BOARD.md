# BOARD — DG 3.0

**Current sprint:** none open

| ID | Title | Layer | State | Lane |
|---|---|---|---|---|
| DG-001 | What actually drives the Engine B projection | 3 | answered — **qualified by DG-017** | crew forensics |
| DG-002 | Walk-forward validation for RB, WR, TE | 3 | todo | — |
| DG-003 | Give the projection a distribution, not a number | 3 | todo | — |
| DG-004 | Stale leakage FAILURE report in repo root | 3 | **done — stale one-off artifact deleted; systemic guards live** | ClaudeFable5-DG004-20260825 |
| DG-005 | Separate player talent from team environment | 3 | todo | — |
| DG-006 | Model the career arc as a sequence | 3 | todo | — |
| DG-007 | Learn when to act, not just what things are worth | 3 | todo | — |
| DG-008 | Opportunity Delta · Market Disconnect · Efficiency Sustainability | 3 | todo | — |
| DG-009 | Regime shifts — new coach, scheme, quarterback | 3 | todo | — |
| DG-010 | Position-specific age curves | 3 | todo | — |
| DG-011 | Stress-test outside the training distribution | 3 | todo | — |
| DG-012 | News, injury reports and pressers as features | **1** | todo | — |
| DG-013 | A feature store you can query as-of a date | **2** | todo | — |
| DG-014 | Deployed models were fit on 2018–2021 only | 3 | todo | — |
| DG-015 | Model card contradicts the training code | 3 | todo | — |
| DG-016 | TE "validation report" doesn't say what it validated against | 3 | todo | — |
| DG-017 | We validate a scaled model and deploy an unscaled one | 3 | finding CONFIRMED, fix unbuilt; **FALSIFIER RUN 08-28 night (report-only, `69b6c194` on ticket/DG-017): fired — usage weight 9→30% (QB), 0.3→12% (RB), 3→32% (WR), 1→35% (TE) under scaled+tuned fit, accuracy within noise; DG-001 attribution falls as football-fact, stands as artifact-description; stakes re-priced = honesty, not RMSE** | — (lane retired, branch pushed) |
| DG-018 | Make "does our model beat the market?" a standing measurement | **3** | **LANDED `5a2bfc12` 09-04 — NOT LIVE until the next pull.** New paired scorer + runner + 14 tests (8 red first); gate 6885/33 + frontend 93/649. The realized-outcome loop CANNOT answer this ("market data never enters", scorer:10) — this is the missing half. Frozen set = David's own 2026-08-05 declaration, drift-checked (501 + 12,209 exactly as declared). Coverage on the card FACE: 501 model / 474 market / **304 paired** / 197 model-only / 170 market-only. ⛔ Brief's kickoff deadline was FALSE — `_build_outcomes` rebuilds every finalised week each run, nothing perishes; earliest real number is **week 4** (4 games + 10 players). ⛔ Ledger is 35,645 predictions, not 866,990. ⛔ `OutcomeIdentityBridge` is FORWARD-ONLY (sleeper→gsis); a reverse-lookup hedge would have no-opped silently all season. | closed — Fred |
| DG-019 | Market appears to over-disperse by ~2× | 3 | todo | — |
| DG-020 | Get more than four market snapshots | **1** | landed — merge `ac8ac4a4` 08-28: 4 → 480 dates (dp_archive monthly 2021-02→2025-06 commit-anchored + fc_history_api daily 2025-07→08-27); DB INSTALLED to trunk 08-28 10:31 — verified 08-29: sha `ad17d82d…`, 480 distinct snapshot_dates on trunk; adversarially reviewed pre-land (provenance labels fixed) | — |
| DG-021 | 114 players told an Engine A prior was used when none exists | 3→6 | **done — merge `b291107f` on `main`, DEPLOYED 10:21: live artifact 114→0 false rows, same 114 now honest** | ClaudeFable5-DG021-20260825 |
| DG-022 | Players with no canonical id can never be graded | **2** | **done — merge `20807368` on `main`: frozen-prediction membership lane, real-surface-QA proven** | ClaudeFable5-DG022-20260825 |
| DG-023 | Health gate labels good participation data "empty" | **1** | **done — merge `b4662707` on `main`** | Parallel-DG023-20260825 |
| DG-024 | PPG counts **ALL GAMES**, postseason included — David 2026-08-19 | 3 | **decided** | — |
| DG-025 | Ablate usage features under a scaled, tuned fit — the deciding test | 3 | **ANSWERED 09-04 by DG-162 §2b — NEGATIVE.** StandardScaler on the same leak-free folds: FULL scaled r² QB 0.351 / RB 0.598 / WR 0.659 / TE 0.607, every arm within noise of the shipped unscaled fit, and **no position beats a 3-column model detectably once scaled** (QB −0.001 [−0.077,+0.067], RB +0.006 [−0.014,+0.023], WR +0.008 [−0.010,+0.028], TE +0.015 [−0.008,+0.040]). Scaling redistributes weight (DG-017) and does not improve the forecast. ⛔ Does NOT license adding the scaler: it moves every published number without improving any of them. | Bob |
| DG-026 | Training labels and test labels share the 2023 season | **3** | done — LANDED `e7391650` 09-04 (Bob), with DG-027. Admissibility follows the LABEL window; feature season 2021 dropped. Stricter variant available but NOT default — measured to leave 1 fold and 80 QB rows. ⚠ r² drops are confounded (leak + data both removed) | — |
| DG-027 | The penalty is chosen by random CV on repeated-player panel data | **3** | done — LANDED `e7391650` 09-04 (Bob), with DG-026. Penalty now chosen on expanding-time folds clustered on player (3 folds/position); **QB 1000 (grid CEILING) → 1**, RB 500→10, WR 200→0.1, TE unchanged. REFUSES rather than falling back to random. ⛔ nothing retrained or promoted | — |
| DG-028 | "We changed nothing" check cannot see the artifacts it guards | 3 | landed 08-28 night — seeing guard hashes all 10 registry artifacts + serving-binding checks (manifest hijack, v1-fallback scan — both review-found BLOCKERS closed RED-first); v1 fallback custodially registered + backup-covered | — |
| DG-029 | Is feature season 2024 absent by design or by gap? | **2** | **done — BY DESIGN, same mechanism; merge `849f3eaf` on `main`** | ClaudeFable5-DG029-20260825 |
| DG-030 | Compare model families to each other, not just to naive | 3 | todo | — |
| DG-031 | Salvage the outcome resolver and Coverage contract | 1 → 3 | **done — merged in PR #159** | CodexCrew20260819 |
| DG-032 | Six ledger trees, three versions of the same day; reconcile the channel | process | todo | — |
| DG-033 | A producer can abort and still be graded fresh | **1** | **done — merge `30a91c33` on `origin/feature/outcome-loop-week1`** | ClaudeOpus5-DG033-20260824 |
| DG-034 | Backup health reports `ok` while the backup is failing | **1** | **done — 52e7dfc9, hand-landed on `feature/outcome-loop-week1`** | ClaudeOpus5-DG034-20260823 |
| DG-035 | Capture chain silently does not run unless David is logged in | **1** | option (b) CLOSED by DG-044; option (a) DEFERRED post-season (David 08-26) | — |
| DG-036 | A failed backup can leave the previous run's `completed` marker standing | **1** | **done — merge `de551d22` on `origin/feature/outcome-loop-week1`** | ClaudeOpus5-DG036-20260824 |
| DG-037 | No ticket can land through `dg-land.sh` — five mechanisms | process | **done — 5 mechanisms fixed, dry-run green** | ClaudeOpus5-DG037-20260823 |
| DG-038 | `dg-land.sh` cannot merge into any base that is checked out somewhere | process | **done — detached merge + `HEAD:$BASE` push; dry-run now proves the merge; gated by `tests/test-dg-land.sh`** | ClaudeFable5-DG038-20260824 |
| DG-039 | A blocked roster-capacity audit writes nothing; last week's audit stands as current | **1** | **done — landed 08-26: always-written status marker, artifact keeps preserve-last-good** | ClaudeFable5-DG039-20260826 |
| DG-040 | The daily nflverse capture has never once succeeded — upstream renamed contracts `cols` | **1** | **done — merge `6b5dceb9`, deployed to the trunk, capture run green same day** | ClaudeFable5-DG040-20260824 |
| DG-041 | The inputs gate is permanently red — participation can never serve the season it is asked for | **1** | done | ClaudeFable5-DG041-20260825 |
| DG-042 | David's "all games" PPG ruling is honoured by accident, not enforced (SR-21) | 3 | **done — merge `c2b11f0a` on `main`** | ClaudeOpus5-DG042-20260825 |
| DG-043 | Player card two-lane furniture fails contrast, markup, and mobile width | **6** | **LANDED 08-30 `555fb7e4`, LIVE** — dl-of-spans → labeled pairs (run-on raw-key line GONE), divergence literal → tokens, 390px overflow 776→390 PROVEN in-browser; panel fixes: TE caveat speaks prose, caveat names its own source, offset-less dates stop showing tomorrow. ⚠ contrast + mobile remain ungated by automation | Davids-MacBook-Pro-23766 |
| DG-044 | SR-11: the daily capture gap alert — the only detection channel that will exist (absorbs DG-035 option b) | **1** | **done — merge `b1b888be`; INSTALLED + LIVE-FIRE ACCEPTED 08-26 12:00 (banner seen)** | ClaudeFable5-DG044-20260826 |
| DG-045 | SR-09: the dependency-ordered fail-soft daily chain (steps 1-7 BUILT + REVIEWED D4; LANDED + swap EXECUTED D5; step 8 = D8) | **1** | landed — merge `4048f25a` on main 08-27; D5 sitting DONE (12 labels — 11 + catchup-guard; slots 1/1/2/2); SR-09 closes after D8's SR-19 exercise | ClaudeFable5-DG045-20260826 |
| DG-046 | The common-cohort divergence fix is built, tested, and NOT WIRED IN (archive stores raws — recomputable) | **5** | **done — merge `e976b1e2`; PRODUCTION-ACCEPTED 08-26 14:00 (rebased artifact live)** | ClaudeFable5-DG046-20260826 |
| DG-047 | Morning report staleness caveat cries wolf — was SR-20 substance; weekly section now cadence-aware | **6** | **done — landed 08-26, SR-20 D10 slot FREED** | ClaudeFable5-DG047-20260826 |
| DG-048 | Layer 1 Daily Control — RETIRED on David's ruling 08-26; runner refuses every mode, module lives on as library | **1** | **done — landed 08-26 (incident during build: see ticket)** | ClaudeFable5-DG048-20260826 |
| DG-049 | Extend the capture-gap alert to the two unmonitored event streams (SR-10b) | **1** | **done — landed 08-26; every capture store now has a detection channel** | ClaudeFable5-DG049-20260826 |
| DG-050 | Replay-reproducibility harness: prove snapshot + parser version reproduces normalized content | **1** | landed 08-28 night — 16 streams replay live (19 reproduced / 1 named legacy vintage / 0 mismatch); found+fixed a real §6.2 violation (fc int-volatility hash shape); sanctioned verifier in the five ingestion walls; NOT yet scheduled (ops decision) | — |
| DG-051 | Catalog as single source of truth: generate or mechanically reconcile scheduler, freshness, and backup co | **1** | todo (foundation, 3d) | — |
| DG-052 | End-to-end restore rehearsal with dated evidence; backup class joins per-store health | **1** | todo (foundation, 2d) | — |
| DG-053 | Crosswalk vintages: capture ff_playerids on cadence before the season burns identity truth | **2** | **done — merge `34a9a970` on `main` (pre-freeze per David); plist install rides Thu's launchctl sitting** | ClaudeFable5-DG053-20260826 |
| DG-054 | One versioned name normalizer producing staging keys | **2** | landed 08-28 night — dg_name_normalizer.v1 frozen (39 tests incl. hypothesis properties; sentinel-string + bytes hardening from review, fold-side so idempotence holds); additive only, consumers migrate later, re-keying stays post-season | — |
| DG-055 | Typed facts and the storage pilot: retire the 501-TEXT-column store through the seven proofs | **2** | todo (enabler, 6d) | — |
| DG-056 | Owned bitemporal identity: mint canonical IDs and run the six-step migration | **2** | todo (enabler, 10d) | — |
| DG-057 | Hashed TrainingSpec with load-time verification — serving that can refuse the wrong artifact | **3** | landed 08-28 night — spec-hash sidecars + verify_artifact (refusal / pre_spec_artifact); 10 deployed artifacts grandfathered by measured sha, list pinned byte-for-byte; engine_b/rookie loaders deliberately deferred to DG-058 | — |
| DG-058 | The §8.3 promotion chain: safe JSON artifacts, equivalence tests, and a PromotionReceipt | **3** | todo (enabler, 5d) | — |
| DG-059 | ScoringEnvelope — separate raw inference from PVO assembly | **3** | todo (enabler, 2d) | — |
| DG-060 | Versioned universe snapshot contract for coverage claims (§8.6) | **3** | todo (enabler, 2d) | — |
| DG-061 | Version the scoring constants and declare their calibration-evidence state | **3** | todo (enabler, 1.5d) | — |
| DG-062 | Manager identity reconciliation across the four-season transaction chain | **4** | todo (enabler, 1.5d) | — |
| DG-063 | Manager behavior profiles (§9.1) — the layer's flagship deliverable | **4** | todo (direct, 3d) | — |
| DG-064 | Pick appreciation as a versioned analytical policy, not a static slot curve | **4** | todo (direct, 2d) | — |
| DG-065 | Posture label contradiction on the morning surface (REBUILDING vs UNCLASSIFIED) | **4** | todo (enabler, 0.5d) | — |
| DG-066 | Declared competitive horizon and risk preferences (§9 lane 8) | **4** | todo (enabler, 1d) | — |
| DG-067 | League format and analytic coefficients out of code constants, into versioned policy | **4** | todo (enabler, 2d) | — |
| DG-068 | Per-lane version streams for the league graph (derive from the archived snapshots) | **4** | todo (foundation, 1d) | — |
| DG-069 | The divergence band is an unversioned constant — make every band versioned, evidence-backed, disclosed | **5** | todo (enabler, 1d) | — |
| DG-070 | Build the §10.1 change-event stream over the rebased divergence — and backfill the season from the PIT st | **5** | todo (direct, 2.5d) | — |
| DG-071 | The Evidence Registry: a typed claim store, generalized from the QB-1 program | **5** | todo (enabler, 5d) | — |
| DG-072 | DecisionOpportunity Object and an executable ordered ClaimLevel with fail-closed composition | **5** | todo (enabler, 5d) | — |
| DG-073 | Scenario 2: hold-versus-move for a David-selected player — the first DOO client | **5** | todo (direct, 3d) | — |
| DG-074 | Scenario 4: trade-partner EVIDENCE panels — receipts, not context cards | **5** | todo (direct, 2.5d) | — |
| DG-075 | Read-model store: precompute, publish atomically, serve with receipts | **6** | todo (enabler, 3d) | — |
| DG-076 | Frontend build manifest — source SHA, OpenAPI hash, build timestamp | **6** | **frontend half LANDED 08-30 `6bf4a155`** — dist manifest + status-drawer stamp; panel BLOCKING fixed: dirty-tree builds now emit `source_dirty` instead of a false clean sha (PROVEN live: trunk build stamps source_dirty:true). **NOT done — the health-endpoint sha-vs-HEAD comparison is deferred backend work** | Davids-MacBook-Pro-23766 |
| DG-077 | Complete the resource layer: dedup, stale state, contract_mismatch, receipt invalidation | **6** | todo (enabler, 1.5d) | — |
| DG-078 | Belief archive read model — what we believed about a player, dated | **6** | todo (enabler, 2d) | — |
| DG-079 | Turn release evidence into a gate — goldens, state matrix, CI wiring | **6** | todo (enabler, 1.5d) | — |
| DG-080 | SR-15: Trade Lab search renders results for the WRONG query (stale-response race) | **6** | done | — |
| DG-081 | SR-16: Morning Room hero counts a number David does not act on (his roster's movers instead) | **6** | done | — |
| DG-082 | The catch-up guard's timer dozes off: launchd pends StartInterval during idle | **1** | landed — merge `27ab6af2` 08-28, hybrid schedule (96-slot lattice + interval) + per-label class-(h) lines; guard label swapped same morning; first lattice tick PROVEN 09:02:00 | Davids-MacBook-Pro-20944 |
| DG-083 | SR-10a (pulled to D6): register market_divergence_history — the schedule-drift block | **1** | landed 08-28 — the only unbuilt SR-10a piece (steps 1/2/4/5 had landed via DG-044); capture-health gains StoreScheduleDrift, config v3 chain_step wirings, OpenAPI additive; adversarially reviewed | ClaudeFable5-DG083-20260828 |
| DG-084 | SR-14 (pulled to D6): forward-capture NULL xVAR — record honestly, fabricate nothing | **1** | landed 08-28 — driver reads valuation xvar; daily_diff guards ALL THREE delta sites (third found by pre-land review); 468 historical rows stay honest NULLs; **PROVEN 08-29: 468/583 rows real xVAR; FABRICATION CHECK PASSED on the actual transition morning (model.deltas=0, vintage_changed_no_score_delta)** — Wed 09-02 glance now a formality | ClaudeFable5-DG084-20260828 |
| DG-085 | Drift block presents a stale chain report as a current reading (DG-083 review minor) | **1** | landed 08-28 night — chain_report_stale:<date> basis, null drift fields on any not-today report; report-wide staleness by newest stamp | ClaudeFable5-DG085-20260828 |
| DG-086 | universe_pvo_batch never populates xvar_percentile_position | **3** | landed 08-29 — compute_dvs_pct_batch wired as the one authority (David: "wire the existing calculator"); 388 ACTIVE_B get real dvs_pct tomorrow (>=400 was miscounted — 80 prospects stay NULL by spec); frontend percent() 100x bug disarmed pre-land (panel BLOCKING); coverage gains populated/reference counts; frontend-suite-red-since-08-28 fixture repaired | ClaudeFable5-DG086-20260829 |
| DG-087 | SR-12: product not openable without a terminal (no launchd agent serves the API) | **6** | landed 08-29 (merge `39c61ece`, trunk pull HELD for the sitting) — dynasty-api plist (KeepAlive, ThrottleInterval 60, WD=trunk) + DG-050's weekly replay-verify plist (Mon 12:00) ride-along; guard registration (replay→receipts, api→unguarded w/ penalty-box remedy); README rebuilt; 14 lane tests + panel fixes; BOTH PLISTS UNLOADED until David's sitting | ClaudeFable5-DG087-20260829 |
| DG-088 | Tied DVS scores get split percentiles (changes the ratified formula — David's ruling) | **3** | todo — post-freeze | — |
| DG-089 | Front-page movers not clickable (David's first-user finding) | **6** | landed 08-29 (merge `e68aac07`, dist rebuilt) — every feed row incl. quiet-day roster rows opens the inspector; panel BLOCKING (width:100% row-shatter) caught in-browser + fixed pre-land; visual-smoke harness repaired, first-ever run | ClaudeFable5-DG089-20260829 |
| DG-090 | Daily tape 5px desktop overflow + 46-node contrast debt (pre-existing, exposed by the repaired harness) | **6** | done — Problem A LANDED 08-29 night (merge `232fc0c1`, live on :8000); Problem B transferred to DG-091. ⚠ Discovery for DG-091: daily-open axe gate is NONDETERMINISTIC (3 pass/4 fail over 7 runs; real debt ~39 nodes, 3.97:1) — see ticket | Davids-MacBook-Pro-23766 |
| DG-091 | First-user verdict: frontend "not great" — all four dimensions | **6** | **PROGRAM LANDED 2026-08-30 — FIFTEEN tickets** (4 phase-1 + 3 phase-2A + 6 phase-2B + 2 finishing; all 15 shas verified ancestors of origin/main). Frontend suite 621. ⚠ **CORRECTED BY THE 08-31 AUDIT — earlier rows/closeouts said 10 and 12 tickets, 7 surfaces and 402→575: all wrong.** There are **EIGHT** nav surfaces, and **"zero raw pipeline tokens" is FALSE as plain English** — raw keys sit in `title=` tooltips, which `renderRule.ts:140` deliberately does not audit. **LEAGUE IS STILL UNBUILT (DG-122).** Acceptance = David's word on a season morning, not yet given | — |
| DG-092 | SR-13: block the retracted TE lambda edit and guard the coupled constants (pulled fwd from Wed D9) | **3** | done — LANDED 08-29 night, merge `1d2a5c89`; spec's three proofs passed verbatim; constants untouched; Wed 09-02/D9 FREED | Davids-MacBook-Pro-23900 |
| DG-093 | League Activity strip — David's committed week-1 deliverable, previously ticketless | **6** | todo — IN-SEASON wk 1, COMMITTED ≤09-17 (spec = SR-18 retained body, SEASON-BUILD-SPEC :1465-1511) | — |
| DG-094 | POST /api/trade/analyze returns a trade winner — filed under Ruling 07 as it stood | **6** | **dropped 08-29 late — David's frontend ruling (verbatim in ticket): frontend may state overall recommendations; delta_status is now the signal that presentation consumes** | — |
| DG-095 | Actions ranked by an xVAR scalar — filed under Ruling 10 as it stood (taxi sort, cut_priority, partner_score) | **5** | **dropped 08-29 late — David's frontend ruling: ranked actions are now legitimate presentation inputs; sentinel map (0 forced :297, -1 exempt :267/:318) kept in ticket for reference** | — |
| DG-096 | Replay the 39 historical trades — the spec's RPL-1..4 program (:1571-1579) had no ticket | **5** | todo — in-season wks 1-4 per spec, RPL-1 first; honest ceilings stand: 21/39 groundable, pick fraction may cut further | — |
| DG-097 | Self-consistency clock over the belief archive — INC 1p, explicitly not calendar-gated | **6** | todo — in-season; thin now-slice of DG-078, build AS its first increment | — |
| DG-098 | A1a shadow ledgers A/B — "the only mechanism in the plan that can detect the product harming you" (REV2:97) | **5** | todo — in-season; Ledger B reaches usable n in ONE season, wants an early start + a designed-in touchpoint in the DG-091 rebuild | — |
| DG-099 | Engine A rookie rebuild — the mission's #1 claim rests on a stale 3-feature C/D model with no rebuild scheduled | **3** | todo — OFF-SEASON Jan-Feb 2027, promotes only through DG-058/059 | — |
| DG-100 | nflverse vintage record in NO backup; 30GB raw/ tree invisible to the anti-rot test | **1** | **LANDED 08-30 (merge `874023ab`) + BACKFILL RUN** — incremental additive channel (quiesce gate, `--no-clobber`, fail-closed listing, per-file sha256 verify), anti-rot extended to large non-`.db` trees, manifest rationales corrected; suite 6,516/0; pre-land panel's 1 BLOCKING + 3 major all fixed. Kill test proved append-only holds (8 verified objects, ZERO partial, no false marker). **INSTALLED + BACKFILL COMPLETE 08-30 on David's "install it": plist bootstrapped 07:34, all 1,584 files (31.8GB) offsite sha256-verified, daily run measured at 13s/0 uploads. My "~$0.60/mo" figure CORRECTED by measurement → DG-106** | ClaudeFable5-DG100-20260829 |
| DG-101 | Stale docs present forbidden architecture as current; Databricks bundle declared an every-minute unpaused job | process | **done — merge `ae2309bf` 08-30**: dated supersession banners on roadmap.md + storage-strategy.md; bundle marked RETIRED + schedule PAUSED (kept for `bundle destroy`). **Databricks RETIRED on David's ruling** — workspace job destroyed, 0 jobs remain. **MEASURED: 0 runs ever, live schedule was PAUSED all along (dev-mode auto-pause) — the URGENT billing alarm was latent, never active** | ClaudeFable5-DG101-20260830 |
| DG-102 | dg-land gate is pytest-only (bin/dg-land.sh:92) — blind to frontend breakage; UI suite sat red on main 08-28→29 | process | **done `10562f9` 08-31 08:51** — `dg-land.sh:100` runs `npm run gate` (typecheck · lint · vitest · banned-language · build) after pytest, refuses to merge if npm cannot be resolved; `tests/test-dg-land.sh:118` proves fire + skip. Board row lagged the ticket until 09-02 (Tower). | — |
| DG-103 | decision_supported criteria + outcome finality — standing proposal drafted; both blanks bite at December's Gate-4 read | **5** | todo — RATIFICATION IS DAVID'S (MASTER §8.5); proposal text in the ticket; per the 08-29 frontend ruling this now gates only the BACKEND flag, not the frontend's voice | — |
| DG-104 | CI banned-language linter enforces a repealed presentation law | **6** | **LANDED 08-30 `67cf9f8b`** — presentation bans relaxed, backend evidence-typing bans STAY armed; panel found 3 BLOCKING (prose-bypass of the field gate, false `pass` on five tier-readiness components, a second cordon still armed) — all closed pre-land; PRODUCT.md law text amended | Davids-MacBook-Pro-23766 |
| DG-105 | DG-091 build 1: retire the raw-literal debt + make the visual gate deterministic | **6** | **LANDED 08-30 `bc065e24`** — raw literals converted on 4 surfaces, `--dg-text-muted` 0.55→0.51, axe flake DEAD (0/10). ⚠ HONEST LIMITS: the census is NOT emptied (rawCssAuditBaseline still totals raw_oklch 40 / spacing 70 / radius 33 / font-size 45 across 18 of 21 files); the 4 converted surfaces are visited by NO axe scan (≥4.5:1 rests on token arithmetic); bundles run under prefers-reduced-motion so the default-motion path has no browser a11y coverage; **21 composited-contrast AA failures remain — all `.dg-shell__parked-badge` at 2.89:1** | Davids-MacBook-Pro-23900 |
| DG-106 | contracts writes a 1.6GB snapshot per capture — 19.3GB of 31.8GB, ~51GB/month compounding forever | **1** | todo — POST-FREEZE (capture-path change); options: weekly cadence / content-addressed pointers / gzip; found measuring DG-100's backfill | — |
| DG-107 | Complete the DG-100 install sitting: register vintage-sync with the catch-up guard | **1** | **done — merge `9c020e5c` 08-30, ACCEPTED**: guard now checks 13 jobs, `jobs_unconfigured: []`, **`kicked: []`** (no duplicate run — the reason the land waited for the marker). DG-100's install sitting complete end to end (landing before it exists makes the guard kick a duplicate run); plist itself BOOTSTRAPPED 08-30 07:34 (15 labels, 1 calendar slot) | ClaudeFable5-DG107-20260830 |
| DG-108 | Backend still strips David-facing evidence with the repealed presentation vocabulary (players.py `_contains_banned`) | **6** | todo — DG-104 panel follow-up; renumbered from DG-107 (collision with the peer lane's DG-100 sitting ticket; theirs keeps 107) | — |
| DG-112 | Delete the retired Databricks bundle | process | **done — merge `d5f4ede4` 08-30**, 8 files; safe only after the workspace destroy; suite 6,518/0; local `.databricks/` cache cleaned; `~/.databrickscfg` left alone (David's credential file) | ClaudeFable5-DG112-20260830 |
| DG-109 | The copy dictionary: no raw pipeline key may reach the screen | **6** | **LANDED 08-30 `59bab53e`, LIVE** — one dictionary + an enforced render rule over visible text, aria-label, alt and placeholder, with THREE declared exempt subtrees (`[data-identifier]`, `[data-user-text]`, `[data-quoted]` — an earlier row said two). ⚠ **08-31 audit: `title=` is an UNAUDITED CHANNEL BY DESIGN (`renderRule.ts:35,:140`) and raw keys are actively placed in it on visible prose — see DG-123** | Davids-MacBook-Pro-23766 |
| DG-110 | Players reachable from anywhere: global search and the remaining dead ends | **6** | **LANDED 08-30 `921ec892`, LIVE** — "Find a player…" in the rail + ⌘K trigger, selection sink on every surface, empty first-load inspector fixed, search no longer mutates the persisted trade draft. Panel: 18 fixed (incl. a false a11y receipt from scanning an empty error state) | Davids-MacBook-Pro-23900 |
| DG-111 | Retire the caveat furniture: say it once, in prose, where it matters | **6** | **LANDED 08-30 `002a26bd`, LIVE** — the six-per-page "Descriptive only — not decision-grade" stamps are GONE across all seven nav surfaces; 16 replacement strings recorded verbatim in the ticket. Panel: 16 fixed. ⚠ FOR DAVID: it reworded TRUST_TRUTH_COPY, a constant marked "never free-typed" | Davids-MacBook-Pro-23766 |
| DG-113 | The morning read: a verdict-first front page | **6** | **LANDED 08-30 `58f5016f`, LIVE** — verdict hero + "Worth a look" recommendation cards (David's green light) + green/red movers; debug dump killed; league list no longer repeats his own players. Assembled in a pure module where every clause names the producer line entailing it — **reading the producers KILLED three of the spec's own example clauses as unsupported** | Davids-MacBook-Pro-23766 |
| DG-114 | The navigation shell: five destinations, a player drawer, and a real phone | **6** | **LANDED 08-30 `ba2e25a6`, LIVE** — rail 11→5 (Today/Roster/Trades/League/Track record), parked surfaces out of nav (retires the 21 `.dg-shell__parked-badge` contrast failures), player drawer opens in one press, **390px bottom-tab phone shell** | Davids-MacBook-Pro-23900 |
| DG-115 | Visual identity: type scale, chrome tokens, and direction color | **6** | **LANDED 08-30 `d18c4610`** — display scale 22/28/32/40px + weight/line-height, space 5-6, 3 radius tokens (33 literals → 0), a NEUTRAL chrome family (shell off the data lanes at all 11 sites), `--dg-up`/`--dg-down`. **The verdict-hue ban was RE-POINTED, not deleted** — red/green legal in exactly those two tokens, still fails on buy/sell styling (falsified both ways) | Davids-MacBook-Pro-23766 |
| DG-116 | Trade Lab is visually unbuilt, and the verdict David ruled is missing | **6** | **LANDED 08-30 `d9a89b87`, LIVE** — styled like the product; states both pricings plainly with the disagreement named and **no blended take/pass**, per David's ruling. ⚠ one raw token still on screen inside a labelled receipt (`cross_lane_manual_review_suppressed…`) | Davids-MacBook-Pro-23900 |
| DG-117 | Surface cleanup: sideways scroll, three names, and QA language | **6** | **LANDED 08-30 `ceba40e1`** — Roster Audit 185px + Model Trust 665px page-scrolls FIXED (tables scroll inside their container); one name for value over replacement, routed through the dictionary; QA vocabulary off user surfaces | Davids-MacBook-Pro-23766 |
| DG-118 | Extend the visual evidence gate to every surface | **6** | **LANDED 08-30 `89110a22`** — 3 URLs → every destination at 1440 AND 390 under BOTH motion paths, composited-color axe, content-presence assertion, render rule in-browser, plus a COVERAGE LOCK. ⚠ **08-31 audit corrections: the gate is 25 tests at HEAD, not 23** (DG-119 added a surface after it landed), and **the "8/8 deterministic" figure is wrong — the ticket records 4 consecutive runs, and on the 23-test version; the 25-test gate has never been shown stable.** It also cannot see page LENGTH — it passed League at 35,475px | Davids-MacBook-Pro-23900 |
| DG-119 | The trade partners view is an unstyled dump | **6** | **LANDED 08-30 `6f766032`, LIVE** — "Who to call": framed cards, manager name leading, one producer-entailed sentence each ("You're rebuilding and they're contending. They're deep at RB and WR — exactly where you're thin."); rank words replace the raw 2.091; **page 6,401px → 1,378px (−79%)**; caveat 11 renders → 1; badges 11 → 0. **Honesty core: `activity_recency_score` is a literal 0.0 (never computed) — the card now says so instead of printing "How recently they've traded 0.00", which told a manager these teams had been quiet** | Davids-MacBook-Pro-23900 |
| DG-120 | The receipt layer still speaks pipeline, and David is one click from it | **6** | **LANDED 08-30 `b1c532d7`, LIVE** — receipts now distinguish IDENTIFIERS (raw, copyable — `model_forward_capture`, `capture_health` stay byte-exact) from MESSAGES (prose: "2 of 3 daily capture feeds are in a bad state. Which, and why:" / "missing 1 of 68 days (2026-08-12)"). The render rule now FAILS a snake_case message inside `[data-receipt]`, so this cannot silently return | Davids-MacBook-Pro-23766 |
| DG-121 | Nothing detects a served bundle that has drifted from HEAD | **6** | done (detector half) — LANDED `6e65640e` 09-04 (Bob); `/api/health` carries `served_bundle`, TWO axes (bundle vs checkout, checkout vs origin) as facts with no verdict word; fails empty, never reassuring. ⚠ review died on the spend limit 3× — verified inline, found 2 real defects. ⛔ PRESENTATION OPEN on David's glyph ruling: the machine notices, it does not yet tell him. **NOT live** | — |
| DG-122 | League is an unbuilt 35,000-pixel destination, and the gate called it clean | **6** | todo — DG-091 follow-on, found by the 08-31 audit | — |
| DG-123 | `title=` tooltips are an unaudited channel, and raw keys are being put there | **6** | todo — DG-091 follow-on (`renderRule.ts:140` skips `title=` by design) | — |
| DG-124 | A second scoring term that does no work, one line from the first | 3 | todo | — |
| DG-125 | The availability model: nothing consumes the 638 recovered rows | **2** | **done 09-01 — `4f05dff1` availability model, walk-forward pooled AUC 0.811; `ee57d802` THE HURDLE IS LIVE: served value = P(plays) × E[points given plays]; `cd4a6234` publish sentinel producer** | Greg |
| DG-126 | A retrain and the scheduled scorer can interleave over five unlocked writes | **2** | **done — consumer half `d280530b` 08-31 (first ticket through the repaired `dg-work`/`dg-land` gate); producer `cd4a6234` 09-01 under DG-125** | Davids-MacBook-Pro-89438 |
| DG-127 | `games` has no lag while `ppg` has two, so the gate judges durability on one season | 3 | **done — `c941d785` on `main` 09-01: `games_t_minus_1/2` + `_available` flags, CARRIED NOT CONSUMED (output table only, no model matrix). Reaches the served table at the next producer rebuild — 09:00 09-02; it missed 09-01's by 12 minutes. David ruled 09-01: wait, do not force** | Davids-MacBook-Pro-26544 |
| DG-128 | Rank everyone: the 8-game gate refuses 115 players we already have history for | 3 | **landed `1dff211f` on `main` 09-02 07:43 — RANGE-ONLY cut (David: "push the branch, run the regen, range-only this week"). 11 commits, head `d72f27dc`, per-commit green 6696→6754 / fe 623→629; band `dvs_band_low/high` on every PVO, σ pinned to the served runs (B `20260831T204458Z`, A `20260502T153931Z`, A_v3 `20260524T140748Z`), batch + card regen fail-closed on a moved manifest. NOBODY NEW RANKED: the fill is held on `origin/ticket/DG-128-fill-held` (fde9a5ca). Trunk pulled, `frontend/dist` rebuilt, API restarted 07:44 (pid 4070) on David's "go"; ranges appear after the next run_pvo_refresh from trunk (09:00) — proof `grep -c '"dvs_band_low": [0-9]'` runtime json → 468. Amendment 09-02: v3 TE head carries its own σ 29.7 (not the ridge's 23.6) — landed by David typing the land command after reading the closeout. ⚠ found on the way: trunk's served bundle is a MANUAL `npm run build` nobody owns (ticket needed).** | closed — Fred (davidleess-45) |
| DG-129 | The availability model is not a published artifact, so a CSV edit changes served values | 3 | todo — David 09-01: not now | — |
| DG-130 | A player reads 100% complete and still has no score, and nothing says why | **6** (+3) | **scoped 09-02 15:33 (Tower) — awaiting David's 4 decisions after the 09-03 morning read.** Verified: Wilson `games_t=7` / Allen `4` (gate, `pvo_assembler.py:497-507` nulls the score; the reason exists on the artifact and `roster_auditor.py:210` DROPS it, then `:531-537` stamps the FALSE `no_usage_signal` on a 100%-complete row); Dell has no 2025 row (PRE_MODEL; `copy.ts:641` maps him to the wrong sentence). 114 of the 115 unscored ENGINE_B rows are `games_t` 4–7. Fix = backend TOKEN (`score_withheld_short_season:games=N` …) carried to the audit row + 3 dictionary entries + roster cell/card + `dvs_pct`. One landing, ~1 day. Side finding filed as DG-139. | Tower |
| DG-131 | The capture is dark because a provenance witness was destroyed; accept a documented discontinuity | **2** | **done — `a62cbcf7` on `main` 09-01; fail-closed registry (`app/config/provenance_discontinuities.json`), capture restored and verified `2026-09-01\|12226`; 08-31 is permanently lost; run dir added to the backup manifest** | Davids-MacBook-Pro-17129 |
| DG-132 | Every accuracy figure on screen describes models that were replaced on 08-31 | **2** | **landed `3bc9ecd2` on `main` 09-01 — PRE-FREEZE. Served-vs-published sha256 check fails closed; a test pins misalignment TRUE today for all four positions; plain-prose note on the trust strip. ⚠ NOT YET LIVE: trunk at `0def485d`, API up since 08-31 08:18 — needs trunk fast-forward + API restart, sequenced AFTER the 09:00-chain partition fix (davidleess-a0)** | Davids-MacBook-Pro-37947 |
| DG-133 | The 09:00 PVO refresh has failed every morning since 08-31: consumers select the inference partition by the wrong flag | **2** | **landed `f8995d3d` on `main` 09-01 — PRE-FREEZE. One selector (`features/inference_partition.py`) reusing the assembler's rule, fail-closed tokens, four readers converted, roster index refuses duplicates, routes answer 503. Live table: old mask 1,143 rows/29 dups → 505/0. Suite 6691 passed. ⚠ NOT YET LIVE: trunk `0def485d`, API up since 08-31 — trunk ff → API restart, in that order, sequenced with davidleess-a0** | Davids-MacBook-Pro-32886 (Tower) |
| DG-134 | The forward capture's provenance records `training_cutoff: null` every morning — it reads a `season` column the runtime table lacks (`feature_season`); swallowed KeyError | **2** | **BUILT + AUDITED 09-02, `e2fa691b` on `ticket/DG-134`, rebased onto DG-139's `c62783b1` at 22:23; **LANDED on main `32ceb8fb` 09-03 05:23** (David ran dg-land himself; gate read directly: 6804 passed / 33 skipped, frontend 90 + 629, no failures). **LIVE on trunk 09-03 05:30** — David said run it; pull `a1f1023f..32ceb8fb` (10 files, DG-139's in the same pull), API kickstart **pid 90590 → 50754**, 5 routes 200 (health 0.40s warm), 27 roster rows, 0 frontend files so no bundle rebuild. ⚠ **The first capture to actually write `2023` is the 09-03 09:00 chain** — the derivation runs at capture time, so nothing is proven on screen yet; read `capture_report.status` + row count, not the chain exit. The refusal path has still never run in production.** Root cause confirmed: `record["season"]` vs the live `feature_season` (col 19 of 44 — the ticket said 40), `KeyError` swallowed, so a null was hashed into `provenance_hash` on every row. Fix: the derivation now uses `inference_partition`'s `SEASON_COLUMN`/`ELIGIBLE_COLUMN` and its `season_of`/`is_training_eligible` normalisers (promoted public — `feature_assembly.SEASON_COLUMN` does not exist); an unanswerable table is REFUSED with the bare token `capture_training_cutoff_underivable` (`TrainingCutoffUnderivable`) → driver's own aborted report, nothing appended, `decision_supported false`; legacy refresh restores + aborts at both hash sites; production reaches it via DG-136 → exit 1. Measured on trunk's LIVE runtime pair into a scratch DB: HEAD `{value: null}` / hash `9b8fd7bf…` → DG-134 `{value: 2023, status: derived}` / hash `166f36ea…`, 12,227 raw + 583 joinable rows and EVERY other column byte-identical. The HEAD rehearsal reproduced today's real live hash exactly, and a second run with NO shim (`feature_source=None`, cwd=trunk, driver resolving its own source) produced the same `166f36ea…`. Neither the runtime nor the seed table can trip the new refusal: both derive 2023, 0 of 2,241 eligible rows have an unreadable season. ⚠ The ticket addendum's "exactly ONE spurious `vintage_changed`" is WRONG, and so is the same claim for DG-139: the vintage is the PAIR (semantic_output_hash, provenance_hash) and **it flipped on 9 of 9 consecutive capture dates** — 14 distinct pairs over 10 dates. A new vintage every morning is the status quo. Mechanism (found by `davidleess-0b`, verified here, now **DG-141**): `resolve_provenance_subset` hashes the microsecond-precision `source_snapshot_captured_at` directly at driver.py:158, so provenance_hash can never repeat across days — while the same function's docstring at :139-141 says the subset excludes dates. The SEMANTIC half works (09-02's two captures share a provenance hash but differ semantically — DG-137's 14:50 rerun, 142 team labels); it is the provenance half that poisons the pair. Pinned: old `season` spelling refused (one fixture moved), zero-eligible → honest `None`, one token carries no sub-cause. 18 new tests, red 15/17 on the first cut; the 18th came from the audit and is the only test in the repo that reaches the post-refresh restore handler (line-traced). Suite on the rebased tree 6804 / 33 skipped, 6786 without the new file — delta exactly 18. Audited before David read it: 18 agents / 6 lenses, 12 findings, 11 refuted with evidence, 1 survived and was FIXED (not reworded); no lens died on the spend limit. Going live: no npm build; DG-134 alone needs only a trunk pull, but DG-139 underneath it needs an API kickstart. | closed — Fred |
| DG-135 | `frontend/openapi.json` does not declare the 503 that `/api/engine-b/scores` and `/api/roster/audit` return since DG-133 | **3** | **landed `60f6940f` on `main` 09-02 15:29 (Tower) on top of DG-137's `862a1afb`.** Premise was FALSE: regen produced a zero-line diff because the 503 was raised at runtime and never declared. Fix = `responses={503: …}` on both routes with models describing the `{"detail": {…}}` envelope FastAPI actually sends, then regen: openapi diff exactly the two 503s + four schemas (+84/−0), generated TS/Zod client regenerated with it. Six contract tests (real 503 body validates against the declared model at all three raise sites; 3 red on old main). Reviewed by 3 lenses + skeptics (10 agents, 0 died): 3 findings fixed before landing. Suite 6772 / fe 629. Exposed DG-138. **Not live until pull + bundle REBUILD + restart** (generated client changed); nothing on screen changes. | closed — Tower |
| DG-136 | A capture-stage `abort()` exits 0 — a returned refusal reads green at the chain level; the standalone label did it twice on 08-31 (the hole was named only by the next day's 10:30 store-hole alert) | **2** | **LIVE on trunk 09-02 21:34 — landed `a1f1023f` on `main` 21:29 (David ran push + dg-land himself; built `11646a09` 20:32–20:57 by Fred on top of DG-135's `60f6940f`); on David's "go" Fred pulled trunk 21:33 (`862a1afb..a1f1023f`), rebuilt the bundle (`index-C6XzDCYI.js`, for DG-135) and kickstarted the API → pid 90590; 5 routes 200; first scheduled run to execute it is the 09-03 09:00 chain.** Fix shape (a), both paths: one helper `_capture_refusal` judges the RETURNED driver report — only a literal `status "ok"` clears the capture stage (fail closed: missing status / non-dict abort too); outer `status aborted / aborted_stage capture / aborted_reason` = the driver's own, `restored_from_backup false`, pair never rolled back, driver untouched, guard untouched. `main()` → 1: chain step `failed`, 10:30 gap alert names it, 11:30/14:00 label red (a 14:00 refusal also shows at the NEXT 10:30 as `exited 1 on its last run`); **System Health shows pvo_refresh `producer_failed` and the shell pill reads "Attention" on such a morning even though the served pair is fresh** (core_substrate rollup — the shape-(a) tradeoff; David may want a distinct card state later). Guard keys on receipt mtime → `plan_kicks` plans nothing (pinned); no chain step hard-depends on the step → market-divergence + what-changed still run. Same-day repeats return `ok` (today's 4 runs) so afternoons stay green. 7 contract tests (4 red on old main, re-verified by 2 auditors), suite 6778 / 33 skipped. Audited before David read it: 30 agents, 13 findings, all confirmed and folded in (08-31 attribution chain→standalone label, a fabricated build time, a false "read-only symlink"). Not induced live. Live (restart was for DG-135's client, not this ticket). | closed — Fred |
| DG-137 | Served team label is the 2025 feature-season team, not Sleeper's current team — 189 valued players disagree, in cutdown week | **2** | **landed `862a1afb` on `main` 09-02 07:47 (Tower) on top of DG-128's `1dff211f`.** One rule in one place — `served_team()` in `universe_pvo_batch.py`, imported by the roster audit: Sleeper wins whenever its block carries the `team` key (`None` = Sleeper says no team now); the model's `nfl_team` only fills a block with NO key, which the snapshot builder never writes. Detail card serves "FA" only for Sleeper-**Active** no-team players (5,567); Inactive/IR/PUP/NFI no-team keep the blank (3,511) — David may overrule. 12 new contract tests (7 red on old code), one old pin flipped (`test_surface3_pvo_preservation` KC→FA). Suite 6704 / fe 623; OpenAPI byte-identical, no regen. Measured: 3 of David's 27 roster rows change on the audit at restart (Mitchell IND→NYJ real move; Mendoza LVR→LV, Black SFO→SF abbreviation). **LIVE 09-02: trunk pulled to `862a1afb` + API restarted 14:30 (Fred, David's "go", pid 95078) — Mitchell NYJ / Mendoza LV / Black SF on the live audit; standalone refresh kicked 14:50 on David's word (receipt ok/ok, `vintage_changed` flipped once) — served ≠ Sleeper team on the artifact 142 → 0 across all 12,227 rows, Demercado → DAL, 468 bands unchanged. Verified by Tower on the machine.** | closed — Tower |
| DG-138 | Three routes (`roster_capacity`, `model_scoreboard`, `realized_outcome_scorecard`) declare a flat 503 model while the wire carries `{"detail": {…}}` — the four `JSONResponse` routes are honest | **3** | open — filed 09-02 15:15 by Tower from DG-135; no live surface reads the body (frontend gates on status), so post-freeze is fine; recommended fix = return `JSONResponse` like the honest four, regen must be a zero diff; ALSO carries the roster 422 `roster_config_error` — undeclared since 04-30 and the one error branch the frontend distinguishes (`RosterAudit.tsx:40`) | unclaimed |
| DG-139 | Served age is the model's feature-season age, not today's — 324 players a year young (255 scored); Wilson's row says 25 AND "2 years to the 28 cliff" | **2** | **landed `c62783b1` 09-02 22:18 (Tower)** — `served_age()` in `universe_pvo_batch.py` + `roster_auditor.py:242`, 8 contract tests, 1 pin flipped. Reviewed by 3 lenses + 7 skeptics (10 agents, 0 died on the relaunch); commit message corrected for 2 unnamed consumers + a wrong path + "all by exactly one year" (318 of 324). NOT LIVE: audit row needs pull+restart, card/League Pulse need the next green refresh. Side finding filed as DG-140. | Tower |
| DG-140 | The card's age verdict is computed from the stale feature age — 97 rows state a cliff verdict false at the player's real age (46 rostered, Wilson among them); 33 past their cliff with no risk flag | **2** | **landed `6f517027` 09-03 06:53 · LIVE 09:00:47** (97->0, counter-arguments 16->0, past-cliff 108->141). Review caught a blocking defect (counter_argument is derived from risk_flags) + a crash risk + a false "one place" claim, all fixed pre-land. NEAR MISS: trunk was 2 behind at 08:05; David pulled 08:06. Was — filed 09-02 22:2x by Tower from DG-139's review (found by 3/3 lenses, reproduced by 5 skeptics). PREDATES DG-139. **Land before the 09:00 chain and the contradictory pair never reaches a screen** — the card only rebuilds on a green refresh. Beware the circular import: `roster_auditor` imports `served_age` from `universe_pvo_batch`. | unclaimed |
| DG-141 | `vintage_changed` is true every morning by construction — a microsecond timestamp is hashed into `provenance_hash`, poisoning the PAIR across days; the semantic half was believed safe and flipped daily too — David ruled BOTH halves out | **2** | **LANDED `ea067645` 09-04 08:52 ET — NOT LIVE until the next trunk pull (before 09:00 or after 14:00, never between; no manual refresh after the pull).** Both halves on David's rulings (Q11 09-03 "take the timestamp out"; 09-04 08:11 "B"): the league clock left `provenance_hash`, and ONLY `lineage.sleeper_snapshot_hash` left `semantic_output_hash`. ⛔ The first B commit dropped the whole `lineage` block and silently took `governance_version` out of the vintage entirely — a governance bump would have read quiet; caught by 5 review lenses at full vote, fixed before landing. **A ruling names a field; do not widen it to the block.** Live proof under the landed code: 09-02 Sleeper hash swapped in leaves `7780ec5ca61c` unchanged, `governance_version` bump moves it. 47 of 47 mornings were new since the daily league run began 07-16. Gate 6872/32 + frontend 93/649. Two panels (54 + 74 agents; 8 refuters died on API errors and were hand-checked, not counted as refutations). Open, not in ticket: the trip sentence still says "rebuilt on a newer model run" (David's wording call); `vintage_changed` asks "seen ever" vs the spec's "vs last captured" (receipt unaffected). | closed — Fred |
| DG-142 | The roster page says three positions "passed accuracy checks" that were never run on the served models — the comparison is a generic label, so the stale branch is unreachable | **2** | **LANDED on main `1812a5c8` 09-03 08:08 on David's "land 142" (hold released early — safe, because landing alone changes nothing on screen). NOT LIVE: trunk unpulled, page still shows the old chips.** Gate read directly: 6829 passed / 33 skipped, frontend 90 + 629, no failures. The staleness guard compared `model_version`, which is the generic `engine_b_v2` on all four positions, so it was unreachable by construction. Now asks `check_served_alignment` (sha256 of the deployed bundle) — the guard the trust surface already used, wired into its second consumer. Refusal unconditional; the two hashes choose only the caveat (both present = `trust_status_stale`, anything unreadable = `trust_status_unavailable`) so it never asserts a cause it did not establish. No new copy. Live effect, all four: EXPERIMENTAL + `trust_status_stale`, roster read `degraded` — authorised, and it includes QB (Bob's "three VALIDATED" undercounts by one). 13 tests, 10 red first; suite 6817/33 vs 6804 without the file. Audit (30 agents / 6 lenses, 24 findings, 16 refuted, 8 survived) changed the work three times: ⛔ **"a backtest re-run restores the badges" was FALSE** — the published surface is pinned by `PINNED_RUN_IDS` in `publish_trust_surface.py:22-27` and enforced at `validate_trust_publication.py:147`, so the follow-on needs a tracked-source edit **in another lane's files**, and alignment ≠ a good badge (the gate outcome on the new bundles is unmeasured); the caveat conflated five causes; and the `caveat → degraded` step was pinned NOWHERE (mutation-proven: deleting it left all 6815 tests green — now caught). ⚠ Landing alone shows nothing: needs trunk pull + API kickstart, no npm build | closed — Fred |
| DG-143 | Lower the season-games gate 8 -> 4 so a player who missed most of a season still gets a number (114 unlocked) | **2** | **LANDED `22d904b0` 09-03 23:40 on David's "yes land it" — NOT LIVE (trunk `6f517027`, 8 behind)**; gate comment corrected to the audited market check (`5426ffbd`); percentile side-effect put to David in plain words first | Davids-MacBook-Pro-32886 → landed by Greg |
| DG-144 | Remove the range: one number per player (David 09-03 06:22 ET: "plus or minus 20, remove it") | **6** | **LANDED `505027b5` 09-03 23:26 — NOT LIVE (trunk `6f517027`, 5 behind; David: "tomorrow is fine")** — on-screen removal only; band fields stay on the API/PVO; 5-lens/3-refuter panel, 21 findings recorded, blend caveat + width claim fixed in `b3cb7c34` | Davids-MacBook-Pro-54105 |
| DG-145 | Free agents show "FA" on the card (David 09-03 05:59 ET; RULED 23:35 ET: "FA" = nobody in the league owns) | **6** | done — LANDED `024de1ac` 09-04 07:29 ET (Bob); card serves `league_ownership` dated by the roster capture, word minted once in copy.ts; **NOT live** until trunk pull + API restart; 4 findings for David in the ticket (two-FA collision on 5,950 cards, 3 self-contradicting) | — |
| DG-146 | A small rookie marker on the roster row (David 09-03 23:35 ET: "small marker indicating theyre a rookie") | **6** | **LANDED `12023dff` 09-04 07:30 — NOT LIVE (trunk 14 behind)**; "Rookies" filter label; flag is derived on the roster route → DG-147 | Davids-MacBook-Pro-54105 |
| DG-147 | A rookie the league did not draft gets no number and no "Rookie" word on the roster, while his card scores him | **3** | done — LANDED `c7ac3484` 09-04 (Bob); index admits any rostered rookie (255→263, the 8 names), `is_prospect` = Sleeper `years_exp`, `draft_class` served; review fix: the unscored caveat named the wrong model; **NOT live** | — |
| DG-149 | Two team signals on every player, in different spots: NFL team (FA when none) and league team (FA when unowned) — David 09-04 07:35 ET | **6** | done — LANDED `3674d940` 09-04 (Bob); "FA" minted once for header/roster row/movers, `league_ownership.team_name` keyed on the MANAGER; ⚠ review died on the spend limit, verified inline (found + fixed the roster-slot join); **NOT live** | — |
| DG-150 | After DG-141 (B), the morning flag's sentence still blames a model rebuild | **6** | done — LANDED `248cddc7` 09-04 (Bob). Sentence → fragment **"no compared player moved"**, no cause named (the payload distinguishes four). Renders in ONE receipt line; the line above already carries the vintage fact. ⛔ mark half NOT done — three enums share that line, vocabulary is the design's call, raised not guessed | — |
| DG-151 | `vintage_changed` asks "have I ever seen this pair?" (store-wide, no date bound) while spec §5 defines it as "vs last captured" — the two could not disagree until DG-141 removed the daily clock and the Sleeper list hash from the vintage | **2** | open — filed 09-04 by Fred at Greg's request. NOT a David ruling; latent discrepancy DG-141 made reachable. ⚠ The morning receipt is UNAFFECTED: `daily_diff` compares the two latest dates directly and never reads this flag. Options: bound the lookup to the prior date; or correct the spec to "seen ever" (cheapest, nothing needs the other reading today); or report both. | unclaimed |
| DG-152 | Schedule the model-vs-market grade (DG-018) and make its weekly job FAIL LOUD on finalised-weeks-with-no-outcomes rather than no-op | **3** | open — filed 09-04 by Fred at Greg's request, right after DG-018 landed. ⛔ NOT urgent and not for week 1: a player needs 4 games and a position needs 10 such players, so the **earliest real figure is week 4**; and nothing perishes by waiting (`_build_outcomes` rebuilds every finalised week from nflverse on each run). Prefer adding a step to the existing Tuesday 10:00 realized-outcome job over a second plist — they share the frozen declaration, the finality law and the outcome builder. ⚠ On that artifact's `auxiliary` tier a `noop` counts as SUCCESS, which is how an undeclared freeze could have read healthy all season — do not inherit that. Notes carry the DG-018 near-miss verbatim and the **third instance** of one shape (with DG-136 and DG-142): a check whose failure path is indistinguishable from its success path. | unclaimed |
| DG-153 | Nothing reports how the model does on a player it has never seen | **3** | done — LANDED `43c15699` 09-04 (Bob); run report carries `metrics_unseen_players`, defined against FITTED rows, REFUSES below 30 unseen players or a >0.40 interval. **QB is WITHHELD** (26 players, interval spans zero) — my filed "-0.158 / worse than average" is CORRECTED to "cannot say"; WR drop was .682→.612, really .679→.673. Direction holds, magnitudes were wrong | — |
| DG-154 | The season rollover fires unattended ~09-15 and rebases every ranked player from a 17-game to a 4-game measurement | **2** | **GUARD LANDED `6b0a9f90` 09-04 — NOT LIVE until the next pull. ⛔ David's A/B/C ruling STILL OPEN; the guard decides nothing.** The basis is now compared against what the live runtime says it was built on and REFUSES on a change unless a dated declaration names that season. No-op today (both 2025), verified on the live marker + 77 pre-existing contract tests. Gate 6929/33 + 93/649; 22 tests, all red first. ⛔ **BOTH FILED NUMBERS WERE WRONG:** loss is **23% pooled** (QB 35 / TE 33 / RB 26 / WR 26), not a third; the "three quarters" figure is a DIFFERENT (inverse-variance) measure giving 69-73% at EVERY position, so it is not a QB finding. 505 is exact; **291 is a point the refill passes ~week 6-7**, not a state (199 wk4 · 282 wk6 · 446 wk18); like-for-like end state is 457-501, mean 480. The rollover also GAINS 2024 as training data. ⛔ Review blocker: the first cut's refusal marker **was read by nothing** — now written into `feature_refresh_latest_report.json`, the artifact `report_freshness.json` actually monitors. Also fixed: failed OPEN on a corrupt ready-marker; wrote `ok` before publishing; wiring pinned only by source greps. | closed (guard) — Fred |
| DG-155 | Capture the 2026-09-08 consensus snapshot before the annual series breaks | **1** | **LANDED `974f49ae` 09-04 — code not live until the next pull; the 2025 SNAPSHOT IS ALREADY ON DISK.** ✅ Lawfulness PASSES on primary evidence: `github.com/dynastyprocess/data`, **GPL-3.0 read from the repo itself**, read-only git clone (not scraping), absent from the prohibited registry, David's sign-off already on record in `verify_dynastyprocess_source.py`. Checked by Fred 09-04. ⛔ **BOTH FILED PREMISES DIED, and are rewritten in the ticket in place:** there is **NO deadline** (the four files are git-history extractions; history is immutable back to 2019 / 361 commits, and the producer reproduces all four byte-for-byte as `already_captured_identical`), and **2025 was NOT a permanent gap — it is CAPTURED** (commit `06e357e2`, 2025-09-05, sha256 `e999a745…`). **Four points became five; 2026 makes six and is HELD until after 09-08 per Greg's ruling.** ⚠ Real risks have no clock: upstream disappearing / history rewritten. Measured: a blobless clone is 1.4 MB but does NOT survive disappearance; a full mirror did not finish in 10 min. **The archive copy IS the defence** — do not build a mirror. Gate 6944/33 + 93/649; 15 tests, 8 red first. | closed — Fred |
| DG-156 | The morning freshness dot carries its state in colour alone | **6** | done — LANDED `cfffd0c1` 09-04 (Bob). Shape carries the state: ● Current / ◐ Limited / ○ No reading. **The base is now HOLLOW** — `unknown` had no rule and rendered as a filled dot, so a check that could not answer looked fine. Dot stays `aria-hidden`; the sentence remains the accessible channel (Greg overruled his own rule 2). **NOT live** | — |
| DG-157 | Derive the cross-positional value from the UNCAPPED score — 17 players are priced at a ceiling, not at their own number | **3** | **LANDED `f80e0309` 09-04 18:5x ET — NOT live until the next pull.** David's decision one. Verified after the change on the live artifact: **0 displayed scores move; 17 cross-positional values corrected; 16 distinct figures where there were 3.** Nacua 39.40→79.40, McBride 2.85→41.67, McCaffrey 58.05→79.60 (upper bounds). **Scope widened by one:** Engine A had a clamped player too (Love, 57.61), so its scorer now returns the pre-clamp score and BOTH engines are corrected. ⛔ NO CONSTANT MOVED — DG-092's 14 tests pass unchanged, which is why it landed alone and why none of Bob's three coupled pieces trigger on it (all are DISPLAYED-score driven). ⚠ **103 of 502 change cross-positional RANK — 17 moved + 86 leapfrogged whose own number did not** ; that rank sorts cut advice and the cards look identical. David told before ruling. Gate 6973/33 + 94/654. | closed — Fred |
| DG-158 | The four score-unit constants that break when the scale moves | **3** | done — LANDED `af6ff61b` 09-04 (Bob), **dormant by design**; Fred's core lands last as the switch. Threshold as a share of the ceiling, prospect gate told of a DECLARED scale, movers capped at 25, band guards, units-change refusal. ⚠ the tenth instance of the day's defect shape was BUILT here and caught by reading — "Projections held steady" on the one morning every score changes | — |
| DG-159 | One scale for every position AND both engines | **3** | **LANDED `b6b9ab3d` + `96dad300` 09-04 21:5x ET — NOT LIVE** (trunk unpulled; the served universe rebuilds on the next chain run). Gate 7071/33 + frontend 95/661, OpenAPI zero diff. **David ruled BOTH: anchor 20.1 and both thresholds** — the ranks were filed here as out of scope and he merged them. One denominator `DVS_SCALE_ANCHOR_PPG` for all four positions and BOTH engines; thresholds RB 33→29, WR 53→45 (QB/TE already right); lambdas all 1.000; one replacement table 61.0/45.2/45.0/41.8; three sigma tables re-derived. ⛔ **`ENGINE_B_P90_PPG` is deliberately NOT the denominator and must NOT be edited to become one** — overwrite it and the score is identical while two DG-158 guards go silent with every test green. ⛔ **THIS TICKET SAID '~10 ranks too deep' AT RB: WRONG — it is FOUR (33→29); the ten was POINTS. Receiver is the big rank move at eight.** ⛔ **The shipped replacement ppg (12.91/7.29/8.79/8.99) HAD NO SOURCE** — the artifact it cites holds 13.47/8.59/8.65/9.76 and no training season or served population reproduces them; the xVAR test carried its own restated copy so test and code agreed while neither matched anything measured. Now a derived, dated `REPLACEMENT_PPG`. **That stale figure, not the rank, is 4.5x of the running-back fall** (Jeanty 29.67 → 21.37 unit → 14.06 re-derived → 12.42 rank). ⚠ **THREE of DG-158's five guards were wired to a constant this change does not move and would NOT have fired** — the top-asset threshold (would have stayed a hard 80 nothing could reach), the prospect scale token (all 80 cards → undeclared drift → exit 1, boards not rebuilding), and the units-change refusal (**fired at NONE of the four positions on the real move**: two engines = two factors per position, and 1-dp rounding scatters small-score ratios). All three repaired and verified by RUNNING: refresh exits 0 then holds invariance; detector silent on all 70 real mornings and fires at 7 groups on the rescale with 0 per-player fallers. Also caught: the roster-audit replacement sentence would have quoted 6.53 not 9.05; `dvs_p90_ref` on Engine A rows was asserted by the assembler not reported by the scorer (a private rookie ceiling passed every test — that is what `96dad300` fixes); the draft-pick curve was a 4th normalisation site and the calibration audit a 5th. DG-092 successor **mutation-tested: 8 mutants, 7 killed, 1 equivalent.** **On his roster: 21 of 26 change place, Dart takes the top from Jeanty, and FOUR cards change their replacement sentence — Barner up through zero, Kaelon Black / Cooper / Burden down through it. Backs fall 13–17 and QBs fall slightly — LARGER than the ~10-down / QBs-up-4-5 he approved against; direction unchanged, magnitude understated. Reported to Greg.** Ceiling pile-up 18 → at most 1. ⚠ The RB/WR split turns on ONE player at a 0.175 ppg margin (Jordyn Tyson, Inactive, 10.94): counting him = RB29/WR45, excluding = RB30/WR44, worth 1.10 to every receiver. He is counted because availability is already inside the score. Prospect cards and the pick curve rebuilt; the served universe untouched. | closed — Fred |
| DG-160 | Show the reasoning behind replacement level — the unbuilt second half of David's ruling 5, as a BUG DETECTOR not a caption | **3** | **LANDED `2c606831` 09-04 20:26 ET — not live until the next pull.** Gate 7014/33 + frontend 95/661; 19 tests; **no constant and no displayed number changed**. **The detector is a BUDGET, and my first design caught nothing:** per-position bounds are too permissive (any rank is arguable if one position takes every shared place), so it now asks whether the four ranks hold AT ONCE — they demand **48 flex/superflex places from a league that has 36**, receiver alone 28. No flex split makes all four true; nothing in the code is told receiver is the broken one. ⛔ **I nearly shipped it as decoration** — `run_audit_pvo` carries no league block, so the panel would have been silently empty in production with all 19 tests green. Now reads the captured snapshot with the PATH pinned by a test. ⚠ QB is NOT assumption-free: its rank needs all 12 superflex places to be QBs, and 9 of 11 filled ones are. Flex split disclosed as a judgement, never invented. OpenAPI regen verified additive (zero deletions). ✅ **COLLISION RESOLVED 09-04 20:3x ET (Greg arbitrated):** two lanes filed DG-160 independently. **This one keeps the number** — it is landed with DG-160 in the commit, the branch and the test filenames, so renumbering it would falsify a git record. The age-disagreement watch moved to **DG-161** (no code, no commit, cost of the move = one filename and one row). Neither lane renumbered the other; the arbitration was routed through Greg. | closed — Fred |
| DG-161 | The model and the market disagree about age, and which is wrong is unknown | **3** | WATCH (week 4) — chased 09-04 on David's 08-31 "chase it now". **REPRODUCES at UNDER HALF the claimed size** (gap 1.6 yr, not 3.6). ⛔ "the model has an age bias" is NOT supported: model +0.0036/yr and market −0.0022/yr both SPAN ZERO; only their difference [+0.0009,+0.0113] does not. His four youngest (Jeanty, Allen, Burden, Bell, all 22) are all sell-side; Mac Jones 27 buy-side. Only RB detectable per position. ⛔ stake corrected: `decision_supported` FALSE on all 363, copy is descriptive not advisory. Settled by the outcome harness, first number week 4 | — |
| DG-162 | What the model actually reads, and what we hold that it never sees | **3** | **MEASURED 09-04 (Bob) — no code changed.** The four Engine B models read **three columns**: `ppg_t`, `games_t`, `age`. Leak-free walk-forward vs a 3-column model: **QB +0.030 r² CI [−0.027,+0.088] and RB −0.000 CI [−0.012,+0.011] BOTH SPAN ZERO**; WR +0.010 [+0.001,+0.019] and TE +0.008 [+0.000,+0.017] real and tiny. Drop test on the live 505: **24 of 62 feature slots cannot move any card by 0.5 DVS** — `snap_share` max .09/.003/.23 (dead in 3 of 4), `epa_per_dropback` .02, `dakota` .01, `aging_curve_value` dead in ALL FOUR while raw `age` is top-5 in all four, `tprr` dead in both carriers. ⛔ **NOT the missing scaler — tested and negative (§2b): scaled fit is within noise everywhere and beats 3 columns NOWHERE. This ANSWERS DG-025 and completes DG-017.** Warehouse: 2.72M rows / **411 substantive columns → 6 reach a model**; `nflverse_usage.db` is NOT the feature path (nflreadpy is; NGS is the only route out). ⛔ **§5 RETRACTED same day by the same lane:** the 09-01 games lags are a DELIBERATE carry — the commit body says CARRIED NOT CONSUMED with each reason measured (consuming them moves 227 displayed values and leaks a raw pipeline key into caveat copy), DG-127 is `done`, and **David ruled 09-01 "wait, do not force"**. Intent was deduced from a missing consumer instead of read from the commit body. ⚠ We forecast P(plays) with no injury feature while holding 45,337 injury rows. ⛔ The 38 GB raw archive is NOT slack — ROADMAP-LAYERS records it as unbackfillable 2027 fuel (contracts alone is 23 GB of it). | Bob |
| DG-163 | Does injury history improve the availability forecast? | **3** | **MEASURED 09-04 (Bob) — NEGATIVE, feature NOT built, no code changed.** Coverage guard PASSED (2018-2025 covers the model's 2018-2023; 1,061/1,193 players; 76.2% of usable rows on a report). Adding 1-3 injury columns: **ΔAUC −0.0001 to −0.0005, every CI spans zero**. Adding 6 or 14 is detectably HARMFUL (−0.006, CI excludes zero) — that is parameter cost on 837 players, not injury signal. **WHY: `inj_out` alone is AUC 0.5129, a coin flip; `inj_weeks` is 0.414 — the assumed direction is INVERTED, more report appearances predicts MORE returning, because you must be on a roster to be listed.** A naive build would have seen 'signal' and shipped roster membership as durability. `games_t` alone is 0.7707. ⛔ `week`/`season` are TEXT — `max(week)`=9 lexicographically; the table is NOT half a season, cast it. ⛔ NOT the medical table that ends 2023 — different source. ⛔ Says nothing about next-week availability or severity (untested). Absence is a STATED state (`inj_covered`), never an implied healthy. | Bob |
| DG-164 | How long does production last? The survival curve | **3** | **MEASURED 09-05 (Bob) — design + evidence, no feature built, nothing fitted to market.** ⭐ **FEASIBILITY PASSES: 14,880 cohort-year observations / 925 players / 1999-2025, and 2,728 at the k=5 horizon against the 264-910 rows that cap the feature models (DG-162).** Startable = top QB25/RB33/WR53/TE13 by total REG PPR points — non-circular, NOT the ≥4-games event. ⭐ **THE DECLINE IS FLAT AND THE EXIT IS EVERYTHING: conditional on still being startable, players produce 74-117% of their old rate at EVERY age. Ageing is an exit process, not a fading one — a decay-factor value term models the wrong mechanism.** E[startable seasons in 5]: QB 30-31 **2.72** > RB 24-25 **2.23**; RB 32+ **0.50** (0% at y5). Elite buys horizon: elite WR 28-29 **3.77** vs fringe WR 24-25 **2.09**. **DAVID'S PROBE: McCaffrey 1.48 [0.93,2.00] falls decisively below Gibbs/Bijan 3.33 — the #2 defect Fred showed survives every scarcity treatment; Henry 0.67 drops below Hall 1.66.** ⛔ **It CANNOT put Allen (3.12 [2.38,3.78], n=15) first — that must come from Fred's per-season value; E[seasons] is a DURATION, not a value, and is not comparable across positions.** ⭐ **S(h) PUBLISHED — 59 cells n>=12 in `survival_curves.json`; the toggle needs the VECTOR, not E.** ⭐ **STRESS-TESTED: the elite effect is NOT purely mechanical — distance-from-bar and age are BOTH large and independent (2.3x+ at 28-29 = 64% vs marginal at 24-25 = 33%). Defensible form is 'distance above the bar predicts longevity as strongly as age', NOT 'elite players age better' (a rate claim this design cannot separate from a floor effect).** ⛔ **NACUA 3.84 [3.47,4.20] IS THE HIGHEST MEASURED — he tops BOTH terms, so the combined board does NOT return David's top three; it returns Nacua then Allen/Gibbs/Bijan. Report the disagreement, do not omit him.** ⭐ **THE TOGGLE IS NOT INERT: 66 of 1,711 cell-pairs reorder between rebuild d=1.00 and contend d=0.55, max swing 1.74x. Fred's 'nearly a no-op' was a property of his SIX PROBE PLAYERS (no front-loaded player among them) — the crossing archetype `RB 28-29 elite` (n=23, S=90/63/57/38/12) IS published.** ⭐ **HENRY SOLVED via margin cells + a pooled 30+ band: RB 30+ at 1.7-2.3x, n=24, E=1.40. McCAFFREY STILL UNPRICEABLE at n=8 — and THAT IS THE ANSWER: only EIGHT RB seasons since 1999 have been age 30+ at 2.3x replacement. No precedent, so no curve, and say so.** ⭐ **REGENERATED AT DAVID'S 09-05 BAR (QB45/RB82/WR108/TE42) + postseason excluded: `survival_curves_deepbar_decile.json`, 205 cells / 35 suppressed, SUPERSEDES all three earlier files.** 33,240 cohort-years / 1,854 players. ⛔ **The deep bar COSTS DISCRIMINATION AT THE TOP — at quartiles the RB Q4 bucket spanned 7.0x-12.5x and gave Breece Hall the SAME E as Gibbs/Bijan; repaired with DECILES (Hall 3.68, Gibbs/Bijan 4.24).** Toggle gets STRONGER: 5.3% of pairs reorder, max swing 2.52x (was 3.9%/1.74x). ⭐ **FIVE HORIZONS ARE HONEST: S(5) rests on 6,094 observations / 1,611 players, all directly observed, and this term reads NO aging curve — the DG-162 inert-curve premise does not apply.** ⭐ **MARGIN CELLS PUBLISHED (`survival_curves_margin.json`, 58 cells + 20 suppressed WITH their n) — a structure that never saw the tier cut, confirming good-beats-young independently: **BOUNDED EQUIVALENCE (not a failure to reject): WR 30+ at 2.3x bar MINUS WR <=23 marginal = +0.03, 90% CI [-0.70,+0.69], zero player overlap** — excellent at 30+ buys the same horizon as barely-startable at 23, to within 0.7 of a season, ~a quarter of the 2.85. RB confirms: +1.18 [+0.38,+1.88].** ⚠ **One form DOWNGRADED on the CIs: elite 28-29 (4.16) vs SOLID 24-25 (3.68) OVERLAPS — the separated claim is elite vs MARGINAL (1.81), not vs solid.** ⚠ TE is thin everywhere (n=4-20), do not build on it. ⚠ REG-only outcome differs in SCOPE from DG-024 (a feature ruling) — flagged for David, not assumed, and now MEASURED: **REG+POST changes only 7.6% of startable player-seasons and is NON-DIRECTIONAL (128 in, 127 out), so there is no compounding under-rating.** ⛔ Corrected a conflation before it reached him: **fantasy playoff weeks are NFL 15-17 and are INSIDE the regular season, already counted** — what is excluded is the NFL postseason (19-22), which no fantasy league scores. (Ranking on wks 15-17 alone churns 51.7% of the set — a 3-week sample being noisy, not signal we are missing.) | Bob |
| DG-165 | The horizon term cannot price a rookie, and rookies are the dynasty asset class | **3** | **FILED 09-05 (Bob) — design decision, NOT a lane's to make.** A rookie has no NFL season -> no production -> no margin -> **no cell at any sample size.** ⛔ **All 80 rookies on the board are unpriceable; Jeremiyah Love is INSIDE THE TOP 20.** No threshold fixes it. ⚠ Missed because the coverage harness keyed on (position, ageband) — the band is fine, the rookie fails on MARGIN; it tested the apparatus, not the subject. ⚠ **OPEN AND URGENT: are rookies BLANK on the asset board or ABSENT from it?** Silently omitting them is the standing defect shape. Three options — blank with a reason / borrow Engine A's draft-capital estimator (⚠ a DIFFERENT estimator with a different error; mixing them unmarked makes the board unauditable) / exclude and say so loudly. ⛔ Not a DG-164 defect, not blocked on the bar question, not fixed by MIN_N. | — |
| DG-166 | Four shipped constants had NO SOURCE, and their guard test carried a copy of the same wrong numbers | **3** | open — **instance fixed in DG-159; class SWEPT CLEAN 09-05 — a SINGLE instance, not a pattern** (419 test files vs 219 production files at `96dad300`; detector validated to fire on `f80e0309` first; ⛔ the first run scanned the 17-behind working tree and read a stale hit as live — read `git show origin/main:`). Replacement ppg QB 12.91/RB 7.29/WR 8.79/TE 8.99 existed only as inline comments citing an artifact that holds `13.47/8.59/8.65/9.76`; no training season and no served population reproduces them (4 checked). **The coupled-identity test carried a restated COPY of the same four numbers — an ANTI-test that certifies the error and makes it harder to find than no test at all.** Cost, on Jeanty: stale figures 7.31 points, rank correction 1.64 — the rank everyone argued about was the SMALL half. Ask: *if this constant were wrong, which test goes red, and where did THAT test get its number?* ⚠ scope against DG-061 before starting | — |
| DG-167 | The edge David actually CHOSE — find where the market is WRONG — has been ticketless for 5 days | **3** | open — his 08-31 ruling 2(a), verified at source; "blind spot" appears **0 times** in 163 tickets. ⛔ Four constraints kill the obvious build: edge = a VALIDATED OUTCOME never a divergence (ruling 8); price may never be a model input; the model is **three columns** (DG-162) so we cannot out-model the market on shared information; free ECR already beats Engine B 13/16 folds. ⇒ the edge is having a number on players nobody is looking at and PROVING it after. First increment is a measurement that can come back NEGATIVE (is the market actually worse in its blind spots?). ⛔ first graded prediction arrives week 4 — claim no edge before it exists | — |
| DG-148 | Sleeper's empty-starter placeholder "0" is served as a rostered player | **3** | done — LANDED `144f1dfc` 09-04 (Bob) on David's "you have permission to touch a data capture" (19:34:46Z, verified at source). **Rostered ids 274 → 273; the one that goes is `"0"` and nothing else — not data loss.** Takes effect on the next SCHEDULED capture, no manual refresh | — |

DG-001 through DG-011 came from the independent consultant brief of 2026-08-18, except DG-004,
which Tower found while checking evidence for DG-002.

DG-014 and DG-016 came from crew forensics on 2026-08-18 and were verified independently by Tower
the same night. DG-015 fell out of that verification.

DG-012 and DG-013 also came from that brief but are **not** layer-3 work and are stamped where they
belong.

---

**Dropped**

| ID | Title | Why |
|---|---|---|
| — | | |

---

**Board hygiene, 2026-08-23.** The seven Lane cells above were rebuilt from the ticket files, which
are the side the tooling reads (`dg-work.sh:39`) and therefore the correct side. They had read `—`
here while the ticket files named live lanes — a reader picking work off this board could have
collided with a claimed worktree.

**~~One conflict NOT resolved~~ RESOLVED by David's word 2026-08-28:** DG-017's marker now reads
"finding CONFIRMED, fix unbuilt" in both places — the problem is real (verified 08-18), the fix
(one pipeline object for eval+deploy) is unbuilt and stays in the post-freeze L3 chain.

All nine `ticket/DG-*` branches are on `origin` as of today. Six worktrees (DG-014/020/021/022/023/029)
still predate the DG-037 tooling fix and cannot pass `dg-land.sh`'s dirty-tree gate; everything in
them is now pushed, so remove-and-recreate is safe.

---

**2026-08-24.** DG-033 landed as merge `30a91c33`. **Its ticket had its two artifacts backwards**,
and the correction is worth carrying: `pvo_refresh` was correctly diagnosed but has never once fired
(0 aborts against 126 `ok`), while `feature_refresh` — bundled in almost as an afterthought — is the
one that has actually failed twice, and the ticket's fix would not have seen it. A green ticket is not
the same as a closed hole; check which half of a ticket the evidence actually supports.

`roster_capacity` was investigated on David's word and deliberately NOT changed — filed as DG-039.
`run_roster_capacity_audit.py:101` guards the only write, so its `status` key can only ever say `ok`;
declaring it would have been a gate that can never fire. `what_changed` and `league_opportunity` write
no status at all, so their silence is correct. **Three of the five undeclared artifacts should stay
undeclared** — the ticket's premise that an unread `status` is always a defect does not hold.

---

**2026-08-24.** DG-036 landed. Two things a reader of this board should know:

**`dg-land.sh` still cannot land a ticket unaided — DG-038.** DG-037 fixed the five mechanisms that
stopped a worktree reaching the gate; the merge block behind the gate has a sixth, and `--dry-run`
returns before it (`dg-land.sh:100`), so a green dry run does not mean a landable ticket. DG-036's
merge was completed by hand in a detached worktree, verified byte-identical to the tested tree, and
pushed. **A failed land also leaves the temp worktree, the claim, the branch and `State: todo` behind,
and the next run silently cleans the evidence** — so an interrupted land looks like an unstarted
ticket whose lane is taken.

**The trunk's LOCAL `feature/outcome-loop-week1` is behind `origin`** (DG-036 and DG-033 both landed
by hand). A detached push
cannot move a branch that is checked out. `~/dynasty-genius-product` needs `git pull --ff-only`; it
was not run here because that trunk carries 47 dirty files from other lanes and AGENT-HOOK rule 1
says leave it alone. None of those 47 touch DG-036's four files.

**DG-034 and DG-036 both live only on `feature/outcome-loop-week1`**, which is now 38 ahead / 6 behind
`origin/main` and unmerged, 11 days from the 09-04 freeze. Nobody has said whether that branch is
meant to reach `main` before the season. It is worth someone's word.

**Naming collision worth knowing:** `origin/main`'s PR #160 merged a branch called `ticket/DG-035`,
but its content is the 2026-08-19 governance removal — nothing to do with the DG-035 capture-chain
ticket, which is still genuinely open above.

---

**2026-08-24, later.** Three state changes a reader of the 09:00 notes above should know:

**The landing pipeline works.** DG-038 is done and DG-040 landed through `dg-land.sh` unaided —
the first ticket ever to do so — against a base checked out in the trunk. A green `--dry-run` now
means "this ticket can actually land": it builds the real merge and runs `git push --dry-run`. The
nine pre-DG-037 worktrees still need remove-and-recreate before they can pass the dirty-tree gate
(safe; all nine branches are on origin).

**The trunk moved.** `~/dynasty-genius-product` was fast-forwarded to `6b5dceb9` (dirty-overlap
checked first — none of the 47 files touched the merge). Local `feature/outcome-loop-week1` is
level with origin again.

**2026-08-25.** The open question above — whether `feature/outcome-loop-week1` reaches `main`
before the season — is answered: **David ruled merge, and it merged as `d33c9896` on
`origin/main`.** Ten conflicts resolved (scorecard trio → PR #159's later refinement; the four
frontend seams REGENERATED via `npm run openapi-gen`, never hand-merged; AGENTS.md → charter +
parallel-work protocol union; AGENT_SYNC.md and the 08-18 ledger → main's landed deletion wins).
Gate on the merged tree before push: pytest **6029 passed / 0 failed / zero collection errors**
(the −294 vs the branch is main's landed deletion of 29 governance-test files, verified by
`git diff --name-status`) and frontend vitest **298/298**. Local `main` fast-forwarded to match.
The trunk still runs `feature/outcome-loop-week1` (content-identical to `main` at merge time);
whether the trunk switches to `main` and where future tickets land is an open operational choice.

**2026-08-25, morning. David ruled: all future tickets land on `main`.** `feature/outcome-loop-week1`
is retired as a landing base; the trunk switches to `main` after today's producer window. Under that
ruling: **DG-029 landed on `main`** as merge `849f3eaf` (gate 6037 passed / 0 failed) — the answer is
**BY DESIGN, same mechanism** (`apply_inference_partition`; 2024 is the in-between season of a 2-year
outcome horizon; making it eligible is a model-definition decision, not a backfill). The close also
resolved a contradiction sitting in the 2026-08-19 trunk ledger: the Consultant lane's "gap, not a
design choice" claim (based on 2024 existing upstream) is wrong — upstream presence is consistent
with a downstream partition. **DG-004 closed**: the repo-root leakage report was an untracked one-off
probe artifact (its writer has zero production callers; `adp_sleeper` not in the current matrix);
deleted, with the pre-commit training-CSV guard and feature-gate temporal check named as the live
protection. **Recreate procedure for pre-DG-037 worktrees is proven**: remove worktree → delete local
branch (origin keeps the copy) → clear Lane → fresh `dg-work.sh` (base `main`) → cherry-pick the one
pushed commit. DG-029's cherry-pick hit one add/add conflict on the 08-19 ledger — resolved by
keeping the trunk's canonical version, NOT re-planting the worktree-local duplicate (DG-032).
**In flight:** DG-023 (handed to David's parallel session, worktree prepped on `main` at
`~/dg-wt/DG-023` with its pushed half-fix cherry-picked, 7/7 contract tests green).

**2026-08-25, later morning. DG-021 landed** as merge `b291107f` on `main` (gate green after
rebase; local full suite 6042 passed / 0 failed). The dead-window no-A-no-B arm no longer claims
an Engine A prior: `dvs_engine` stays None, the caveat says outright that no score is available,
and the player API serves a degradation notice on any modeled row with a null score. Two spec pins
of the false behavior (phase15 5.10, phase14 5.5) amended with disclosure comments.

**2026-08-25 10:21 — THE TRUNK IS ON `main` AND THE LIVE CARDS ARE HONEST.** Post-window, on
David's "go ahead": three dirty files on main-deleted paths archived
(`preserved/2026-08-25-trunk-switch/`) and stashed, `git switch main` → `b291107f` (dirty 47→25),
pvo-refresh hand-run with launchd's exact invocation → `status ok`, exit 0. Acceptance on
`universe_pvo_runtime.json` (12,225 rows): false rows **114 → 0**, old caveat **114 → 0**, honest
no-score caveat on **exactly 114** rows, all `dvs_engine=None` — the cohort is conserved. The
11:30 scheduled run is the remaining launchd-triggered confirmation. **All future producer runs
now execute from `main`.**

**The 06:15 capture is fixed and proven.** DG-040: upstream had renamed contracts `cols` →
`season_history` and added `contract_history`; the scheduled job had been 4-for-4 exit 1 since
install, and the last normalized contracts vintage was 2026-08-08 — a 16-day gap, not the 2-day
gap the err.log suggested (the log is younger than the machine). Store migrated additively, a
hand-run of launchd's exact invocation finished `status ok / exit 0`, and a 48,690-row contracts
vintage dated today sits beside the 08-08 ones. Tomorrow's scheduled run is the last confirmation.

---

**2026-08-25, mid-morning. DG-023 landed** as merge `b4662707` on `main` (gate 6067 passed / 0 failed,
ruff clean). The finding worth carrying: **the producer half already cherry-picked onto that branch
(`f2e09ab`) was inert.** It set `status: "loaded"` for participation, but the READER bucketed a stream
as empty on `status != "loaded"` **OR a null season** — and participation's frame has no `season`
column at all, so it stayed in `EMPTY:` regardless. Before/after output was byte-identical. A green
producer-side fix is not the same as a fixed surface; run the reader.

Both of this ticket's false words are gone and both were re-measured, not inherited:
`load_participation(seasons=[2025])` returns **45,184 rows across 26 columns, none named `season`**,
and `fallback_used` means a REFUSED SEASON, not a cache — `nflreadpy`'s `cache_mode` is `MEMORY`, so a
scheduled run starts cold and never serves anything from cache. The gate itself is untouched: empty,
unavailable and step-back all still degrade.

**DG-041 filed, not fixed.** Participation's upstream ceiling is `current_season - 1` *by
construction*, so it steps back on every run forever and holds `feature_refresh` degraded
permanently — even after Week 1 when the other four streams go live. A gate that is always red
carries as little information as one that is always green. Fixing it means a per-stream ceiling in
the producer's season window, which moves `source_hash`; David scoped it out of DG-023 deliberately.

**Landed alongside a live second session** (deploy/verify lane, waiting on the 11:30 pvo-refresh).
Checked first: no `dg-land.lockdir`, nothing in `doing`, zero file overlap with the trunk's 25 dirty
paths, and **`pvo_refresh` does not declare `input_provenance_field`** — only `feature_refresh` does,
of eight artifacts — so this change cannot move what that session was about to verify. `dg-land.sh`
again refused to touch the trunk and printed the note instead: **the trunk's local `main` is now 1
behind `origin/main`** and wants `git pull --ff-only` when its tree is quiet.

---

**2026-08-25 11:38 — DG-021 production-CONFIRMED, and the trunk is current.** The 11:30 scheduled
pvo-refresh fired from launchd's own trigger: exit 0, report `status ok`, artifact vintage
`15:30:02Z`, and the acceptance held — **0** rows with `dvs_engine="A"` beside a null score, **0**
old caveats, **114** honest no-score rows. Nothing about DG-021 remains open. The trunk was then
fast-forwarded `b291107f → b4662707` (overlap 0 of 25 dirty paths), so DG-023's reader fix is live
for tomorrow's 06:15+ producers and every job now runs from current `main`. Board state: DG-021,
DG-023, DG-029, DG-004 all closed today; next unclaimed picks are DG-022 (WIP on origin), DG-041
(new, layer 1), DG-020, and SR-11's slot tomorrow 08-26 — sequencing is David's call.

---

**2026-08-25 17:1x — DG-041 CLOSED (code), landed `b797ee1f`, trunk pull deliberately held.**
David's word: "go" on the DG-041-first sequencing. The fix is the ticket's cheapest shape taken
exactly: a per-stream source CEILING beside the floor in `_STREAM_LOADERS`, participation's being
the client's own formula (`get_current_season(roster=True) - 1` — verified as literally the first
line of the installed `load_participation`). TDD, RED watched (3 expected failures); one DISCLOSED
test change in the CH1 isolation fixture; gate 6,070/0, zero collection errors. Known and
disclosed: `source_hash` moves once on first run (provenance echo — frames byte-identical), then
settles. **Sequencing in force: the trunk stays on `main@a61f0fbe` through tomorrow's ~09:00 cycle
so DG-023's first scheduled production run is single-variable; trunk pulls post-window 08-26;
DG-041 live from 08-27 with seven runs before the 09-04 freeze. Production acceptance still open:
08-27 report shows participation `fallback_used=false` and `/api/health` reads `inputs_live`.**
Next unclaimed picks unchanged: DG-022 (WIP on origin, rebase care — its WIP touched a test file
DG-021 also changed), DG-020, SR-11's slot tomorrow 08-26.

---

**2026-08-25 19:3x — DG-022 CLOSED, landed `20807368`; DG-043 filed; an ID collision resolved.**
The 08-19 WIP was resumed by its own handoff, rebased (generated seams REGENERATED — the regen
caught `index.ts` about to drop main's newer types), hardened with four measured fixes (duplicate
region landmark on the real surface; TWO test files silently reading the production capture DB —
hermetic seams added, disclosed; unclosed sqlite handle; unpinned classifier arm), and **the
real-surface QA gate the WIP was blocked on now PASSED** — Tank Dell's card tells the truth on
desktop and mobile, evidence in `dg-build/preserved/2026-08-25-dg022-qa/`. Gate after rebase onto
the parallel session's DG-042: green; local suite 6,087/0, frontend 73 files/302, zero collection
errors. Whole-page QA found three PRE-EXISTING base-card defects, proven independent of DG-022 →
**DG-043** (layer 6; Tier-3-shaped, so post-freeze unless David says otherwise). **ID note:** the
a11y filing briefly carried the number DG-042 (the 08-25 product ledger says "Filed as DG-042")
before discovering the parallel session's same-evening DG-042 (PPG guard, `c2b11f0a`) — theirs
was committed first and keeps the number; the a11y ticket is DG-043 and was never committed under
042. **Trunk is now 3 merges behind origin/main** (DG-041 `b797ee1f`, DG-042 `c2b11f0a`, DG-022
`20807368`) — all still land tomorrow's post-window pull; none of the three touches a producer
except DG-041 (the planned one). Remaining unclaimed: DG-020, DG-043, SR-11's slot tomorrow.

---

**2026-08-26 05:5x — DG-044 filed and claimed for SR-11's D4 slot, on David's word.** SR-11 had no
ticket file and `dg-work.sh` requires one; DG-044 carries the claim and the build-morning brief —
the spec section (`SEASON-BUILD-SPEC.md:595-749`, MIG-1) stays the authoritative build text.
Worktree `~/dg-wt/DG-044` cut from `origin/main` `2bf91d8d` (NOT the pinned trunk), landable-clean,
lane `ClaudeFable5-DG044-20260826`; a fresh number was used deliberately — `ticket/DG-035` is
poisoned by the PR #160 collision. The trunk stays pinned at `a61f0fbe` until the post-window pull
(~10:15+); 09:00–10:15 remains hands-off for DG-023's first scheduled single-variable run. Setup
re-verified before filing: both origin tips unmoved overnight, dg-build clean at `748becf`.

---

**2026-08-26 06:57 — DG-044 LANDED, merge `b1b888be` on `main` (SR-11 + SR-10a step 1 shipped D4,
on schedule).** TDD build (76 ticket tests, every unit watched RED), then a 23-agent adversarial
review: 17 confirmed findings all fixed in `f47d3a6f` — headline: the crash guard (an uncaught
exception now DELIVERS a crash alert instead of silently killing the only detection channel), and
per-bootstrap reboot semantics (a mid-morning reboot no longer floods false "never attempted"
lines; unverifiable slots become one consolidated line that still names every job). Live-verified:
production dry-run names exactly the model_forward_capture 08-12 hole and nothing else. Suite
6,177/0. **DG-035 option (b) is CLOSED by this land** (in-suite proof; option (a) stays open as
David's call). DG-040's final confirmation also landed this morning: second consecutive scheduled
06:15 success, status ok. **Also proven this morning: the DG-040 marker briefly reads
`status: running` mid-capture — a transient state readers must tolerate.**

**2026-08-26 midday — the day's sequence EXECUTED, SR-11 fully accepted.** DG-023's
single-variable confirmation cycle ran green on the pinned trunk (fc 09:00 · features 09:15 ·
league 09:20 · model/pvo 09:30:08 · market 09:40:04 · what-changed 09:45; participation upstream
timeout again = expected pre-pull, DG-041's fix now in). Trunk pulled `--ff-only`
`a61f0fbe → b1b888be` ~10:16, zero dirty-path overlap. Capture-health verified in-process
(TestClient; no server was on :8000, so no restart — next API start serves DG-022's surface):
config_version 2, `market_divergence_history` registered with its 4 missing dates — spec:1011
substance confirmed, SR-10a step 1 accepted. David missed the 10:30 bootstrap window; recovery
was bootstrap + `launchctl kickstart` at 12:00 — a real launchd-path run: runs=1, exit 0, alert
file carries the model 08-12 GAP line + the market 4-date GAP line + heartbeat, state schema 2
persisted, stderr empty. **David saw the banner** — visible-notification acceptance met and the
08-25 probe question closed. Remaining proof is passive: **tomorrow's scheduled 10:30 run must be
silent + heartbeat** (calendar scheduling). 08-27: DG-041 production acceptance unchanged.

**2026-08-26 afternoon — the post-noon game plan EXECUTED (adversarially reviewed first, per
David's ask; his "go" ~12:40 covered the three-ask batch).** Rulings recorded: (h) amendment
RATIFIED; DG-035 option (a) DEFERRED post-season (ticket updated); SR-09 steps 1-5 pulled to D4.
Executed: Tuesday-1 baseline preserved (`preserved/2026-08-26-tuesday1-baseline/`, commit
`75a8f67`) BEFORE the chain rewire makes it unrecapturable; **DG-045 filed and built — SR-09
steps 1-5 complete, TDD, 26 tests, worktree suite 6203/0, commit `bf5ba8a1` pushed on
`ticket/DG-045`, NOT landed (install + retirement are D5/D6, David's launchctl)** *[superseded
same day: steps 6-7 prepped `ab522db7`, 29-agent review fixes `6038d2d6`, 44 DG-045 tests green —
the DG-045 row above and the ticket file are current]*; hygiene sweep:
`agent/modeling-backend` pushed (last unbacked branch), 63 untracked trunk files protectively
copied (`preserved/2026-08-26-trunk-untracked-copy/`), stale Air lane claims cleared on
DG-014/015/020, clean worktrees DG-015/031/035 removed (tips verified on origin; DG-014/020
worktrees left in place — dirty, another day's classification). DG-045 ticket carries the full
resume brief + the D5 stale-report observation.
