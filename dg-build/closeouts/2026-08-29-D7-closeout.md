# D7 closeout — Saturday 2026-08-29: the product's first real user day

**Four lands/executions, the launchctl sitting, two first-user tickets filed, David AT the
machine.** Trunk = origin/main = `e68aac07`; dist rebuilt + served (`index-zM7FmfFO.js`);
14 dynasty labels; all repos level with origin; memory + rulings updated.

## Landed / executed today
1. **DG-086** `c7409cab` — compute_dvs_pct_batch wired (David: "wire the existing calculator").
   Panel BLOCKING pre-land: percent() ×100 disarmed. **LIVE 14:00: 388/388 percentiles in the
   serving artifact** (honest count is 388, not ≥400 — 80 prospects NULL per spec 5.14).
2. **SR-19 rehearsal** (pulled forward on David's word) — outcome (b) CLEAN REFUSAL, proven
   through the chain runner; record + B1 proof + false-alarm shield in the DG-045 ticket.
   **SR-09 close = D8 paperwork only** (finding-A spec amendment, then mark closed).
3. **DG-087/SR-12** `39c61ece` — dynasty-api agent + DG-050 weekly replay-verify (Mon 12:00)
   INSTALLED at the sitting (session hands on his word). First replay run via guard backfill:
   **verdict reproduced, 20 checks**. Frontend dist was a WEEK stale — **rebuild-after-pull is
   now the ritual (README)**. David's acceptance: **"ok now i see it."**
4. **DG-089** `e68aac07` — David's own finding ("no cards are displaying"): movers weren't
   clickable. Built TDD + 3-lens panel same day; panel BLOCKING (width:100% row-shatter) caught
   in a REAL browser, fixed, re-proven (43px rows, click→inspector end-to-end). Quiet-day
   roster rows wired too. LIVE.

## Filed
- **DG-088** — pool tied-DVS percentiles (David: "Pool the ties — file a ticket"); POST-FREEZE.
- **DG-090** — pre-existing 5px tape overflow + 46-node contrast debt, exposed because DG-089
  made the visual-smoke harness run FOR THE FIRST TIME EVER; post-freeze unless pulled.

## Also
- Morning hygiene `11bf67d`: 8 stale ticket headers flipped, DG-020 board row corrected
  (DB install verified DONE — never redo), B1 + two-morning baseline recorded in DG-045.
- GitHub PAT rotated + old REVOKED (David's hands); keychain git auth verified intact; new
  token activates for the github MCP on session restart.
- 10:30 alert: heartbeat + ONE honest line (pvo retry exit-1 = benign first-capture-wins
  refusal; may repeat while afternoon content drifts — watch, don't fix without David).
- GCS backup 10:52: 697 files, sha-verified, **fc_snapshots.db (480 dates) now in the cloud copy**.
- Frontend suite had been RED on main since 08-28 (DG-083 schema vs stale fixture; pytest-only
  gate blind to vitest) — repaired in DG-086's land; 311/311 at close.

## Open going into the tail
- **Tue 09-01 (D8):** 15-min Tuesday check (three weekly jobs' first scheduled post-chain
  exercise) → SR-09 close paperwork (finding-A spec amendment incl. SR-19's own verification
  block) → done-mark SR-09/SR-19 in the spec.
- **Wed 09-02 (D9):** SR-13 (block the retracted TE lambda edit). DG-084's 09-02 glance = formality.
- SR-08 remainder still unscheduled (offered 08-29, not selected). D10/D11 buffer; FREEZE Fri EOD.
- D7 Mon alert-watch: chain-label double-reporting (DG-045 open item b) — observe, only build
  the exclusion if it shows.

## Post-closeout addendum (evening)
**DG-091 filed — David's first-user verdict, verbatim: "to be honest the front end was not
great," all four dimensions selected (looks · density · language · navigation).** Design
mandate, proposal-first: draft the design brief during D10/soak (docs, freeze-safe), David
rules on it, BUILD post-freeze beside League Activity (≤09-17). Venue (crew vs Studio) is his
call. Done = his word on a season morning. Session closed cleanly: all repos level with origin,
no locks, no stray processes, 14 labels; token rotation complete — new PAT arms next session.

## Post-closeout addendum (late night — a second session, David's four rulings)
David asked "what can be done tonight," and a verified survey (7-agent, sources not memory)
put the menu to him. His four picks, verbatim from the option labels:
1. **DG-091 venue: "Studio fresh-eyes."** Recorded in the ticket — the finished brief hands to
   Studio THROUGH Tower (wall TW29-WALL-35 respected); brief drafting pulled forward to tonight.
2. **DG-090: "Overflow fix only."** Problem A (5px tape overflow) pulled to tonight; Problem B
   (46-node contrast) folds into DG-091's visual pass so tokens aren't retuned twice.
3. **dg-mail-carrier: "Retire — unload it."** EXECUTED ~then: `launchctl bootout` + persistent
   `disable` (verified: label unlisted, `disabled` in print-disabled; plist left on disk at
   ~/Library/LaunchAgents/, reversible). It had been firing EVERY 30s into the deleted
   scripts/dg_mail_carrier.py since the tmux era — carrier.log was an unbroken wall of Errno 2.
   Dynasty label count is now 14 healthy + this one retired; "all labels green" is TRUE again.
4. **"Yes — both": SR-13 pulled to tonight** (spec-authorized: "0.5d, no dependencies") **+ the
   SR-09 finding-A spec amendment drafted tonight.** The formal SR-09 close STAYS Tue 09-01 per
   DG-045's own law — tonight is the amendment text, not the done-mark.
Also corrected: freeze day 09-04 is a FRIDAY (this file already had it right; the session
briefing had said Thursday).

## Post-closeout addendum 2 (night session — three lands, a discovery, and a design ruling)
- **DG-092/SR-13 LANDED** merge `1d2a5c89` (gate 6493/0; spec's three proofs verbatim;
  constants untouched, perturbation-proof RED) — **Wed 09-02/D9 is FREED**.
- **DG-090-A LANDED** merge `232fc0c1` (gate 6494/0; overflow-wrap hardening from the panel;
  baselines regenerated in-change) — trunk pulled to `48fa1e7d`, dist REBUILT per the ritual,
  live :8000 verified serving the new bundle (`index-PXhlfPBx.js`).
- **SR-09 finding-A spec amendment PUSHED** `48fa1e7d` (scratch --report-path on both chain
  proofs; SR-19 block superseded by DG-045's executed --step-extra form). **Tue 09-01 is now:
  15-min Tuesday check + the 5-minute SR-09/SR-19 done-mark. Nothing else remains.**
- **⚠ DISCOVERY (DG-091 inherits, recorded in DG-090's ticket):** the daily-open axe gate
  (visual-smoke.spec.ts:453) is NONDETERMINISTIC — 3 pass / 4 fail over 7 same-tree runs;
  failures show the real contrast debt (~39 serious nodes, fg #767a7e on #161c21 = 3.97:1).
  Playwright is not in the land gate; lanes running it on main hit coin-flip failures until
  DG-091 retires the debt. Never exclude the color-contrast rule.
- **⭐ DAVID'S DESIGN RULING (verbatim, ~18:5x):** "I really don't care for the caveats and
  the hard wording governance. I prefer to use prose and layman's language with respect to
  making this a world-class fantasy football dynasty front end. Not a data science, data
  engineering visualization." Recorded as the CONTROLLING PRINCIPLE of DG091-DESIGN-BRIEF.md
  (rewritten same night): governance register retired from the screen; truth stays, spoken in
  prose; no-verdict law untouched; the byte-locked-copy sign-off question is answered by this
  ruling (replacement prose ships in the brief round-trip for his review).
- Parallel session filed DG-093..103 + IN-SEASON-QUEUE.md (dg-build `483dac5`); coordination
  held — zero collisions, DG-092 row was left to this session and is now on the board.

## Correction to addendum 2 (same night, minutes later)
David gave the gap-audit session the same ruling in FULLER words (verbatim in DG-094's drop
record): *"I don't care to persist the governance of language and caveats and lack of overall
recommendation from the back end into the front end. I'd rather use layman's terms and call a
spade a spade, and I've given it the green light to do so."* Addendum 2's line "no-verdict law
untouched" was the narrower single-message reading and is CORRECTED: the FRONTEND is green-lit
to state overall recommendations in layman's terms (supersedes the 08-20 presentation law;
DG-094/095 dropped, DG-104 filed by the gap-audit lane); the BACKEND evidence machinery
(decision_supported, evidence grades, replay, never-fabricate) is unchanged. The brief, the
rulings memory, and this closeout now all carry the reconciled reading.

## Post-closeout addendum 3 — DG-091 PROGRAM PHASE 1 (David: "start building")
Workflow: Tower seat (preflight) + Studio fresh-eyes seat (design spec) + 4 crew build lanes.
**Tower preflight GO** — :8000 serves the current dist byte-identical, trunk level at `48fa1e7d`,
chain 6/6 ok, all four scopes ruled capture-safe (one edge named: DG-076's health-endpoint
clause reaches app/api/routes/system_health.py — backend API, not capture/model/data).
**Studio spec DELIVERED** → `DG091-STUDIO-SPEC.md` + 42 screenshots in
`preserved/2026-08-29-studio-shots/`. Designed from the live surface only; ~/frontend-studio
never touched.
**All four builds COMPLETE and COMMITTED**, branches PUSHED (worktrees are in no backup):
ticket/DG-104 `c850e7e2` · ticket/DG-076 `7da3c762` · ticket/DG-043 `d8189eb9` ·
ticket/DG-105 `896db9ea`.
**⛔ HALTED BEFORE LANDING — USAGE CREDITS EXHAUSTED.** 12 agents failed on
"out of usage credits": 8 of 12 panel refuters (only DG-076's panel completed) and all four
land agents. **NOTHING LANDED; main is untouched at `48fa1e7d` + whatever DG-100 adds.**
RESUME PROCEDURE (fresh session, after credits): for each of DG-104 → DG-076 → DG-043 → DG-105
in that order — run the 3-refuter panel (DG-076's already ran, findings in the workflow
journal `wf_c57737ad-077`), fix BLOCKING findings, then `dg-land.sh <id>`. DG-043 and DG-105
both shrink the raw-CSS census: whichever lands second regenerates
rawCssAuditBaseline.json + visualCraftAuditBaseline.json on the rebased tree. DG-105's
determinism proof (7 consecutive visual-smoke runs) must not run while the peer's ~30GB GCS
upload saturates the machine.

## Post-closeout addendum 4 — DG-091 PHASE 1 COMPLETE: ALL FOUR LANDED + DEPLOYED (2026-08-30 early AM)
Resumed after the credit halt; the 8 dead refuter lenses re-ran, then all four landed serialized
over the peer's DG-100 (`874023ab`):
**DG-104 `67cf9f8b`** — the frontend green light is REAL. Panel found THREE BLOCKING defects in the
first cut, all closed pre-land: (1) the re-scope was applied at gate granularity, so "confidence
score"/"dynasty tier"/"recommended action" could be rendered as label+value PROSE, bypassing the
surviving field gate — closed by a phrase-granular split; (2) five David-ratified
`no_directive_copy` components in app/config/tier_readiness.json would have kept reporting
`pass` for a check that no longer existed — expectations corrected; (3) a SECOND cordon
(scripts/scan_league_opportunity_no_verdict.py) was still armed against two authored frontend
files and still failed closed on the word "recommend". PRODUCT.md's law text amended so the repo
no longer contradicts itself.
**DG-076 `6bf4a155`** — manifest + status-drawer stamp. Panel BLOCKING closed: a build from a
dirty tree would have stamped a commit whose code did not produce it; `source_dirty` is now a
required manifest field. PROVEN LIVE tonight — the trunk build correctly stamps
`source_dirty: true`. **NOT done: the health-endpoint sha-vs-HEAD comparison is deferred.**
**DG-043 `555fb7e4` — LIVE AND VISIBLY BETTER.** The player card's run-on unlabeled string
(`ENGINE_BACTIVE_B77.518.4358%…` / `FantasyCalc5082Overall 30…market_overlay_static_caveat`) is
GONE: labeled pairs in both lanes, prose caveats ("Market values come from a saved FantasyCalc
snapshot, not a live feed."). **390px overflow 776 → 390, verified in a real browser at both
1440 and 390.**
**DG-105 `bc065e24`** — raw-literal debt retired on Model Trust / Roster Capacity / Trade Lab /
Project Tracker; `--dg-text-muted` 0.55→0.51 L; **the axe flake is dead** (0 failures in 10 runs;
51/55/24-run A/Bs agree). HONEST LIMITS recorded by the panel and NOT hidden: all three evidence
bundles now run under `prefers-reduced-motion`, so the default-motion path readers actually see
has no browser-level axe/screenshot coverage; and none of the four converted surfaces is visited
by any axe scan, so ≥4.5:1 there rests on token arithmetic, not measurement.
**DEPLOY RITUAL DONE:** trunk pulled to `bc065e24`, vitest 76 files / **343 passed**, dist rebuilt,
live :8000 verified serving `index-CdQ7ZdFT.js` + `/assets/build-manifest.json` (200).
**Filed:** DG-108 (backend `players.py:_contains_banned` still strips David-facing evidence with
the repealed vocabulary) — renumbered from DG-107 after a collision with the peer lane's DG-100
sitting ticket; theirs keeps 107 (created 07:37 vs mine 07:20, but theirs is referenced in their
lane — cooperative resolution, no external refs to mine existed).
**⚠ RETRACTED — the 4.6s players-API measurement is CONTAMINATED, do not quote it.** It was taken
while 36 orphaned CPU busy-loops from this session's own DG-105 flake-under-load test were still
spinning (load average 63-84 for ~90 minutes; the peer session's contemporaneous 8.7s /api/health
reading is contaminated by the same cause). **FULLY RETRACTED 2026-08-30, including the qualitative half.** My first correction still claimed
"the card does sit on Loading player detail for a noticeable beat — that observation stands." It
does not. Re-measured on a clean machine (load 1.57): **the card renders 199ms after the click**,
and the peer's clean API numbers agree — /api/players/{id} 0.273s cold, 0.135-0.147s warm;
/api/health 0.35s (it read 8.75s under the spinners). **There is no players-API latency problem and
no ticket is warranted.** The lesson beyond the first one: when a measurement is contaminated,
the qualitative impression formed at the same moment is contaminated too — retract the whole
observation, not just its number.
**ROOT CAUSE, worth keeping (this session's error):** in a non-interactive `zsh -c`, `jobs -p` does
NOT return background-subshell PIDs, so the test's `kill $LOADPIDS` killed nothing and every loop
was orphaned to PID 1 when the parent exited — three invocations, 36 spinners, 90+ minutes,
machine-wide degradation. Capture PIDs explicitly (`(while :; do :; done) & LOADPIDS="$LOADPIDS $!"`)
or install a trap. Nothing was lost (pure spin loops, no test state), and the peer killed them.
**PHASE 2 (the restyle per DG091-STUDIO-SPEC.md) IS NOW UNBLOCKED** — linter freed, build stamped,
card structurally sound, gate deterministic.
