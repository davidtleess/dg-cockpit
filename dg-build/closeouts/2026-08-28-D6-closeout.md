# CLOSEOUT — D6, Friday 2026-08-28 (written Sat 08-29 morning)

**The biggest day of the sprint: NINE tickets landed on `main` (27 commits), one report-only
experiment with a fired verdict, six David rulings recorded verbatim — all while David was away
from the machine, every decision through remote panels, every land through the full gate plus an
adversarial refuter review.** Freeze is 09-04; the sprint tail is now only the items that need
David's hands.

## Landed (all merged to `main`, all pushed, all adversarially reviewed pre-land)

| Ticket | What it is now |
|---|---|
| DG-082 | Guard's launchd idle-doze cured: hybrid schedule (96-slot explicit-hour lattice + kept interval); alert class (h) one line per label. **Lattice PROVEN through two idle nights** (ticks 09:02 D6, 08:47 D7 morning). |
| DG-083 | SR-10a's one unbuilt piece: StoreScheduleDrift on capture-health, config v3 chain_step wirings, OpenAPI purely additive. |
| DG-084 | SR-14: driver records real xVAR; daily_diff guards ALL THREE delta sites (third found by review, 115 live rows); 468 historical rows stay honest NULLs. |
| DG-020 | Market history **4 → 480 dates** (dp_archive monthly 2021-02→2025-06 commit-anchored delta-0 + fc_history_api daily 2025-07→08-27). DB installed to trunk 10:31, sha `ad17d82d…`, original rows byte-identical, QB-1 record provably unshiftable. |
| DG-028 | Seeing no-mutation guard: hashes all 10 registry artifacts + serving-binding checks. Review found 2 BLOCKERS (manifest-key hijack exits 0; unregistered served v1 fallback) — closed RED-first; v1 fallback custodially registered `5c52f811…` + backup-covered when the anti-rot contract fired at the gate. |
| DG-057 | TrainingSpec sidecars + `verify_artifact` refusal / `pre_spec_artifact`; 10 grandfathered shas pinned byte-for-byte; engine_b/rookie loaders deferred to DG-058 deliberately. |
| DG-050 | Replay-reproducibility harness live across 16 streams: 19 reproduced / 1 named legacy vintage / 0 mismatch — and it FOUND+FIXED a real §6.2 violation on first run (fc int-volatility hash shape). Sanctioned as the five ingestion walls' second exemption. **NOT yet scheduled — ops decision open.** |
| DG-054 | `dg_name_normalizer.v1` frozen (39 tests incl. hypothesis properties; sentinel/bytes hardening done fold-side after the property suite caught 'ÑA'→'na' idempotence breakage). Additive only; re-keying stays post-season. |
| DG-085 | A stale chain report can no longer impersonate a current reading (`chain_report_stale:<date>`). |

## The experiment (report-only, ruled so by David; branch `ticket/DG-017` @ `69b6c194`, pushed, NOT landed)

**The DG-017 falsifier FIRED.** Scaled + honestly-tuned refit: usage-family weight 8.9→30.4% (QB),
0.3→12.4% (RB), 3.4→31.6% (WR), 1.4→35.1% (TE); accuracy within noise both ways; all four deployed
pkls replay-verified byte-faithful. **DG-001's attribution falls as a fact about football, stands as
a description of the shipped artifacts. Standing law from this: never quote this model family's
coefficient shares as "what drives production" — attribution is fit-dependent.** DG-017's unbuilt
fix is re-priced: honesty/attribution, not RMSE. Report:
`docs/experiments/2026-08-28-dg017-scaled-refit-falsifier.md` on the branch.

## David's rulings recorded (verbatim in the rulings memory)

Morning panel: build all three (guard fix / SR-10a / SR-14) · **DG-020 INTO THE SPRINT ("Yes,
start now")** · class (c) third alarm SKIPPED · DG-017 marker = "problem confirmed, fix not built".
Evening panel: all four night lanes · **"Land the safe ones tonight"** (experiment report-only).

## Verified world state at closeout (Sat 08-29 ~09:00)

- First post-swap mornings: **two for two, all green** — every capture on the dot, chain 6/6 both
  scheduled fires, 10:30 alert pure heartbeat (08-28), guard lattice ticking through idle nights.
- Backups: GCS daily sha-verified; cockpit nightly auto-commit ALIVE (22:00 on 08-27 AND 08-28 —
  first auto-commits since 08-09), preserve-then-scream exiting 1 loudly on the autonomy lane's
  still-red WIP (that lane's to fix, preserved as-is).
- Repos: product + dg-build + dg-cockpit all level with origin. Product trunk carries only the
  known standing dirt (3 modified + 23 untracked, all preserved in dg-build/preserved 08-26).
- 12 dynasty launchd labels; wake 6:13; machine on AC, never sleeps.

## Open items, owner and date

1. **Sat 08-29 09:00 chain** — first capture with real xVAR (same-day re-runs correctly refuse on
   immutability). Result recorded below.
2. **Wed 09-02 morning, BEFORE anything else: THE FABRICATION CHECK** — `daily.model.deltas` must
   be small (single/low-double digits), NOT ~468. If ~468: revert the DG-084 driver same morning.
3. **D8 (David's hands):** SR-19 rollover rehearsal (command in the DG-045 ticket) closes SR-09;
   SR-12 "app openable" (David loads the agent).
4. **SR-13** (D9, block the TE lambda edit) — last scheduled sprint item; D10 pure buffer.
5. **DG-086** (filed): universe_pvo_batch.py xvar_percentile_position — blocks real dvs_pct.
6. **DG-050 scheduling** — the replay harness is launchd-shaped but deliberately uninstalled;
   whether it joins the schedule is an ops decision for David.
7. Cockpit autonomy WIP is red (adapter drift + failing tests) — its lane's; dg-mail-carrier
   last exit 2 — unowned observation.
8. DG-017 worktree parked at ~/dg-wt/DG-017 (branch pushed; Lane cleared).

## Sat 08-29 09:00 verification — OBSERVED, both proofs PASSED

**xVAR records for real:** chain 09:00:01→09:00:54, all six steps ok. Capture 2026-08-29:
583 joinable rows, **468 with real xVAR** (range −101.65..+58.05, mean −14.7) — the exact 468
scored rows, after 64 days of NULLs. `dvs_pct` still 0 as documented (DG-086's upstream defect).

**THE FABRICATION CHECK PASSED — on the real transition morning, four days early.** Today was
the actual None→real vintage boundary (08-28 NULL-xvar → 08-29 real-xvar). The what-changed
report: `model.deltas` list length **0**, `model.status = vintage_changed_no_score_delta`,
`vintage_changed = true`. The DG-084 guard emitted ZERO fabricated moves where the pre-fix code
would have printed ~468. The Wed 09-02 scheduled glance remains as a formality; the event it was
scheduled to catch has already happened and passed.
