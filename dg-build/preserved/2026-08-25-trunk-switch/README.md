# Preserved 2026-08-25 — trunk switch to main

These three files carried uncommitted working-tree modifications in the shared trunk
(`~/dynasty-genius-product`, then on `feature/outcome-loop-week1@6b5dceb9`) on files that
`main` DELETED in the 2026-08-19 governance removal. `git switch main` refuses while they
stand, and they are not this lane's work, so their exact working-tree content is preserved
here (and in `git stash` in the product repo) before the switch. The committed versions
remain reachable in product-repo history via the merge `d33c9896`'s second parent.

Provenance: content byte-copied from the trunk working tree at 10:20 EDT, after the
09:00–10:15 producer window closed, on David's "go ahead" (trunk switch to main ruling,
2026-08-25 morning).
