# First candidate review and next assignments — 2026-09-06

David reported that all three Claude sessions were idle. Verified from session metadata and their final responses: each completed its first bounded increment and was waiting for Codex. This is a continuation of David's authorized build, not a new permission request. Continue in the three existing isolated ticket worktrees; preserve all checkpoints and run artifacts.

## What the review established

Reviewed candidate tips: DG-177 `09eee65a`, DG-165 `9a6a7a4b`, DG-178 `da210bdf`, each against `ecc260ef`. Independent focused test runs passed: 47 veteran, 20 rookie, 85 ranking. These tests establish the implemented behavior, not scientific validity of the assembled player values. None of these candidates is approved for live ranking publication. No active dg-autonomy run record was found in DG-178; this is a direct code/evidence review, not a completed autonomy run.

DG-177 correctly closes its outer two-year outcome window and reproduces the earlier experiment. DG-165 replaces the old unequal-follow-up 'ever' study with explicit historical horizons. DG-178 gives the producers a shared interface and preserves player rows. The following corrections are required before drawing stronger conclusions.

## Lane 23481 — DG-177: correct historical validation, then produce actual annual forecasts

Keep ownership of veteran evaluation, availability/production validation and the season-outcome adapter. Do not edit the rookie or ranking packages.

1. Correct the verified inherited unclosed-label splits in `eval/backtest_harness.py:463,553` and `models/availability.py:196–201`. Correct inner tuning too: `scripts/train_engine_b.py:372–374` currently admits labels not closed at the inner validation date and `:657–665` imputes before that split. Fit preprocessing within each training fold. Reuse a shared explicit cutoff rule where practical. Test that changing a label unavailable at forecast time cannot change that earlier fitted forecast or tuning result. Preserve a named old-recipe reproduction arm.
2. Correct the opportunity denominator or rename it to its actual quantity. `opportunity_features.py:73–83` counts only weeks present in that source, not all games played; 1,061/3,337 joined rows differ from `games_t`. Preserve observed zero-opportunity appearances and distinguish unavailable data from zero. DG-024 explicitly approves ALL-GAMES historical Engine B PPG; postseason inclusion is not a defect and must not be silently changed. Any REG-only fantasy valuation target is a separately named candidate with its own denominator and evidence.
3. Build report-only year-1/year-2 forecasts on explicit seasonal events, not the existing two-season union. Expose aligned unconditional season points and appearance/exposure information as available, and any P(event), conditional rate pair only when the exact event matches. Preserve zero-appearance outcomes, incomplete-horizon censoring, forecast dates and units. Keep existing all-games evidence intact; a separate REG target may use all-games historical inputs without claiming it is the old served target. Coordinate the exact target definition with DG-178 before generating a combined value.
4. Evaluate the new procedure exactly as final scoring fits it; report annual probability calibration and production errors against actual training-only baselines. Aggregate top-k metrics by forecast season instead of taking the top k over concatenated years. State the narrow experiment result accurately: the tested raw-rate Ridge additions showed little gain; this does not prove opportunity information has no value. Bootstrap uncertainty conditional on existing fitted models is not all sources of model/season uncertainty.

Next handoff: corrected evaluator plus honest annual candidate outputs/metadata for the ranking lane. No promotion is required to complete this implementation. Longer horizons stay explicitly unsupported until measured; do not repeat a two-year average through them.

## Lane 24974 — DG-165: validate the probabilities actually consumed, and resolve unknown labels

Keep ownership of `rookie/`, its runner, tests and candidate artifacts. Draft-position eligibility is settled; no new user approval needed.

1. `evaluate.py:151–175` never directly grades exported annual `p_qual_year_j`. Evaluate and save these predictions against `qy_j`. Historical evaluation and final scoring must use the same label-specific maturity/training procedure (`evaluate.py:69,151` versus `model.py:139–145`). Build the historical fit once per forecast date using only labels then known, and grade every consumed annual quantity.
2. Do not turn missing career evidence into known failures. `cohort.py:69` maps missing `games` to zero; all 107 no-ID prospects described as proven washouts actually have `games=NaN` in the frozen input. Verify documented upstream semantics or independent career evidence. Preserve unresolved prospects explicitly and quantify label sensitivity; neither drop them silently nor assert a negative without evidence. Do not restore the missing-age shortcut.
3. Enforce coherent cumulative events through a defensible construction and evaluate the resulting probabilities. In the frozen 80-player output, all 80 have at least one decreasing cumulative P(played by h), and six have decreasing P(qualifies by h). Separate fits in `model.py:183–191` do not make valid nested-event probabilities. Do not repair only a displayed count while leaving contradictory probabilities underneath.
4. Pool the actual historical training-baseline predictions; `evaluate.py:200–210` instead uses pooled test prevalence. Generate all report numbers from the resulting evidence. The frozen REPORT quotes prevalence 0.17 at h1 and 0.37 at h6, while its JSON says 0.216 and 0.416.
5. Remove unsupported composition assurances. Qualification by season total is not the same as contributing useful partial-season weeks, and Engine A's >=8-game condition is not P(played >=1 game by h). Supply annual forecasts/targets that preserve nonqualifier contribution, appearance/exposure and compatible units. Coordinate with DG-178; do not force all nonqualifiers to zero. Keep draft-capital qualification probabilities as useful descriptive forecasts in their own right.

After these corrections, test a bounded explanation/correction for the observed level bias (e.g. recency or year trend), using historical validation with tuning inside training windows. This is a statistical experiment within scope, not a question about spending money. Keep the simpler model if the evidence does not support a change. The scientific prerequisite corrections above come first.

## Lane 25057 — DG-178: make compatibility enforceable, repair the actual code defects

Keep ownership of `ranking/`, its adapters, tests and coverage audit. Other lanes own forecast estimation.

1. Separate numerical coverage from scientific comparability. A string describing an event or caveat does not make incompatible quantities compatible. The current veteran adapter uses P(t+1 OR t+2) for h0 and scales a PPG margin by season-total retention; the rookie adapter uses P(total-rank qualification) times the clipped conditional mean-rate margin. These are research approximations, not verified unconditional contributions. Their presence must not qualify a value for a comparable ranking. Keep legacy research output reproducible, but make incompatible inputs fail the comparable-value path with a reason and preserved player identity.
2. Agree a minimal annual target contract with both forecast lanes: season/forecast cutoff, scoring scope, event, exposure/denominator, quantity and replacement assumptions must match. Define the value estimand mathematically and test its assumptions. Include the nonqualifier branch and games missed when claiming expected contribution. `max(E[X]-R,0)` is not `E[max(X-R,0)]`; an ex-post best-player selection is not an attainable weekly lineup policy. Do not fix incompatible events by matching prose strings alone.
3. Fix fractional-age binning: 23.5, 25.5, 27.5, 29.5 and 31.5 currently fall through to `32+` in `survival_cells.age_band`. Test interval edges on actual floating ages, with a stated rule matching the cells' training population.
4. Fix rookie forecast-clock validation: a 2026 rookie file currently accepts forecast_date 2027, labels rookie year1 as 2027 and calls it full. Refuse incompatible vintages or implement a properly validated elapsed-season update; do not silently shift the same prior forward. Reject non-finite terms (NaN is currently accepted).
5. Fix the lineup counterfactual's duplicate-player case: adding a QB who is already on the roster currently duplicates him and can add points. Enforce player identity and test add/remove semantics.
6. Replace the blanket future-zero rule in `veteran_served.py:85–93`: being below replacement today does not establish six years of zero contribution. Seven rostered players currently receive these full zero paths. Use a measured horizon forecast or keep the unsupported future explicit; do not equate a missing positive-margin cell with certain failure.
7. Re-run the roster audit after corrections. Distinguish supported comparable values, research-only approximations, incomplete inputs and no forecast. The old '25 full' count was not a count of scientifically validated player values. Reconcile the denominator against every roster ID: `audit_roster_coverage.py:134–140,199–201` records missing identities but excludes them from coverage counts, and dictionaries silently overwrite duplicate Sleeper IDs. Resolve Dart/Tank Dell only with appropriate evidence or an explicit stated absence; do not use a nearby survival cell or invent a value. Demonstrate completeness of the eligible replacement pool; best scored replacement is not automatically best obtainable replacement.
8. Remove the unsupported claim in the write-up that rookie-leading order cannot be optimistic because conditional levels underpredict. Qualification calibration and rookie/veteran comparability are separate. Validate the assembled contribution target on historical withheld seasons after correcting the producer inputs; passing adapter tests alone is insufficient.

Keep five- and six-season views parameterized; a choice of displayed horizon is not a reason to stop this work. The possible need for more seasons is separate from whether the current season forecasts are valid. Preserve David's available-replacement decision and one-number preference. Keep league asset value distinct from immediate marginal roster fit.

## How the lanes continue

Start these corrections now in your existing claimed worktree. Work sequentially through your lane's queue; routine checkpoints do not require another start message. Put each useful result and the next concrete step in your ticket. If a dependency is pending, continue independent tests, target adapters and validation instead of going idle. Send interface changes directly to the other two Claude lanes and record the accepted version in your ticket; Codex reads those files and the session responses.

Stop when this review's required corrections and the compatible annual handoff are complete, or when progress truly requires unavailable data/access or a product-direction decision. State the exact issue and all independent work remaining. Do not turn ordinary model testing choices into questions for David. Do not merge, promote models, publish rankings, restart production, change shared data or install dependencies. Codex handles review and the next authorized integration step.
