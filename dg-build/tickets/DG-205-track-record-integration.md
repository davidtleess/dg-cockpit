# DG-205 — track record integration

**Lane:** —
**Status:** PLANNED — reserved; implementation not dispatched
**Reserved owner:** Codex root
**Worktree:** /Users/davidleess/dg-wt/DG-205 (not created)

## Outcome

Own the end-to-end track record milestone and prove that the Lovable screen serves the selected immutable source and correct separate evaluation.

## Ownership

Create the view adapter, Python API route/tests and private opt-in local Lovable bridge/tests named in the plan. Register routes and integrate explicit owner paths. Root alone owns contract changes, board updates, runtime configuration and final source/browser checks. Never rewrite original saved receipts to attach grades.

## Dependencies and handoff

DG206 inputs/store; DG207 policy/scorers; DG208 UI. DG204 is not required for local delivery; required separately for any hosted release.

## Acceptance

- Real preserved DG189 archive verifies and renders through local Lovable with its original save/source dates.
- GET never writes; explicit capture checks the six displayed source fields; stale inputs409 with no write; duplicate preserves first save; partial capture/enrollment success is precise.
- Bridge is disabled on hosted deployments by default, loopback-only locally, with tested Host/Origin/redirect/path refusals.
- Independently replay baseline/grade fixtures and archived bytes; inspect desktop 1440, phone 390 and320 containment, keyboard, axe and network behavior.
- Required isolated gates pass or pre-existing failures are identified exactly. No actual performance result invented. Final handoff distinguishes code readiness, source readiness, outcome maturity and hosted blockers.

## Shared contract and limits

Read [the reconciled implementation plan](../TRACK-RECORD-IMPLEMENTATION-2026-09-08.md) before work. It contains exact schemas, source rules, file ownership, test commands and integration order. Raw planning reviews are evidence, not overriding instructions. Base verified main: `4ad796c223d9b55bf18c3497cd3f69ba35cb7a6d`; reverify before dispatch. Create a fresh isolated worktree using `dg-work.sh` while Lane is unclaimed, then claim it. Preserve all DG200–204 work and evidence. No shared trunk/data/environment edits, no frontend-studio access, dependencies, model changes, historical outcome grading, scheduling, hosted data exposure or publication.

Planning is complete; product implementation has not been dispatched. This ticket is reserved to the stated role, not already running. Root will verify session identity/activity before execution.
