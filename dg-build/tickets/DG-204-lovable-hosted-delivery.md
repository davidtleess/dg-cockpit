# DG-204 — Lovable hosted delivery

**Lane:** Codex-DG204
**Status:** BLOCKED — Lovable workspace credits exhausted

David: “commit push merge”, then “then continue”. DG200 is merged at4ad796c2. Prepare the actual Lovable source and private verified-snapshot delivery path without changing valuations, setting up a second project, or publishing production prematurely. Root owns bounded implementation/integration; Claude54281 inspects supported Lovable source connection, Claude54331 inspects existing backend hosting/auth. Add the missing Lovable CI checks as part of maintaining the adopted interface. Preserve source/history and ignored private data; no live data writes, dependency changes or production deployment. Hosted mutation only when concrete reviewable scope and prior authorization permit it.

Acceptance: reproducible source/build validation; authenticated snapshot delivery and honest failure/refresh behavior, tested against the actual supported hosting mechanism; explicit remaining owner step if the connector cannot perform it. No localhost URL or public personal snapshot substituted for private hosted access.

Current hosted blocker: Lovable rejected the first source message before execution because workspace credits are exhausted. No hosted source/data/deployment changed; latest remains5b487ffd. David was informed. Public-versus-sign-in question remains pending; no data delivery proceeds without it. Root finishes the independent browser CI regression locally.

Local CI/browser regression finished and verified inDG204, uncommitted. Source attempt changed nothing. Final handoff ../DG204-HOSTED-HANDOFF-2026-09-08.md.
